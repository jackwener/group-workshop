# 06 — 功能规格说明（引导版模板）

> **06 只做一件事**：把 `05` 的 User Story 翻译成**前端可实现的界面行为规格**。"谁要什么"写在 05；"界面怎么表现"写在本文。

---

| 项 | 值 |
|---|---|
| 模块编号 | M1-QA |
| 模块名称 | 投研问答助手 |
| 文档版本 | v0.1 |
| 阶段 | Spec（What — UI / UX 行为） |
| 上游 | `05` UserStory 与验收标准 |
| 下游 | → `08` 架构 · `09` API · `13` E2E 测试 |

---

## 1. 页面总体布局

> 画出你的应用的页面结构（ASCII / 手绘均可）。SPA 通常分：Header + Sidebar + Main + Input Area。

**示例 — 布局结构**：

```
┌──────────────────────────────────────────┐
│  header: 标题 + [能力状态芯片]             │
├────────────┬─────────────────────────────┤
│  sidebar   │  main-content（三态渲染）     │
│  历史会话   │  A) 空状态                    │
│  [+新建]   │  B) 常见问题网格              │
│  session1  │  C) 对话历史列表              │
│  session2  │                              │
│            │  input-area: textarea + 发送  │
└────────────┴─────────────────────────────┘
```

```
┌──────────────────────────────────────────┐
│  header: “投研问答助手” + [能力状态芯片]       │
├────────────┬─────────────────────────────┤
│  sidebar   │  main-content（三态渲染）        │
│  历史会话   │  A) 空状态：“请创建或选择会话”     │
│  [+新建]   │  B) 常见问题网格（可点击快捷提问） │
│  session1  │  C) 对话历史列表（卡片式）       │
│  session2  │                              │
│  session3  │  input-area:                  │
│            │    textarea + 发送按钮 + 清空  │
└────────────┴─────────────────────────────┘
```

- **Header**：左侧应用标题“投研问答助手”，右侧能力状态芯片（CoPaw / 百炼 / 离线演示）
- **Sidebar**：会话列表 + “+ 新建”按钮，每条显示标题与删除按钮
- **Main**：三态渲染（空状态 / 常见问题 / 对话历史）
- **Input Area**：multiline textarea + 发送按钮 + 清空按钮

## 2. Header 区域

> 描述 Header 包含哪些元素。重点关注能力状态芯片（对齐 `05` US-005）。

### 2.1 能力状态芯片（Capability Chips）

| 条件 | 芯片显示 | 说明 |
|------|----------|------|
| `caps.copaw_configured = true` | CoPaw 桥接 | CoPaw 已配置 |
| `caps.bailian_configured = true` | 百炼 · {model} | 百炼已配置 + 模型名 |
| `caps.bailian_configured = false` | 离线演示 | 降级到离线模式 |

## 3. Sidebar 会话管理

> 对齐 `05` US-001。描述会话列表的增删查交互。

| 功能 | 触发 | API 调用 | 行为 |
|------|------|----------|------|
| 加载列表 | 页面渲染 | `GET /sessions` | 渲染会话列表 + 默认选中第一个 |
| 新建会话 | 点击"+ 新建" | `POST /sessions` | 插入列表头部 + 自动选中 |
| 删除会话 | 点击 × | `DELETE /sessions/<id>` | 确认弹窗 → 从列表移除 |
| 选中会话 | 点击会话条目 | `GET /sessions/<id>/records` | 设置 `currentSession`，加载该会话问答记录 → Main 切换到对话历史或常见问题状态 |

## 4. Main 内容区（三态渲染）

> 对齐 `05` US-002（问答提交）、US-003（历史记录）。描述三种条件下显示什么。

| 状态 | 条件 | 显示内容 |
|------|------|----------|
| **A 空状态** | `currentSession === null` | "请创建或选择一个会话开始" |
| **B 常见问题** | 有会话 && 无记录 | 显示 4–6 个常见问题卡片网格（如“贵州茅台最新评级？”），点击卡片自动填充到输入框并发送 |
| **C 对话历史** | 有记录 | 按时间升序渲染问答卡片：每条含用户 query、AI answer、来源标签（copaw/百炼/离线演示）、响应耗时 response_time_ms、时间戳 timestamp |

### 4.1 记录卡片中的来源标签逻辑

> 根据 `answer_source` 和 `llm_used` 组合显示不同标签。

| `answer_source` | `llm_used` | 显示文本 | 样式 |
|-----------------|------------|----------|------|
| `copaw` | `true` | CoPaw | 绿色/蓝色标签 |
| `bailian` | `true` | 百炼 | 绿色/蓝色标签 |
| `demo` | `false` | 离线演示 | 灰色标签 |

## 5. 输入区域

> 对齐 `05` US-002 AC（query 非空且长度 1~500）。

| 元素 | 类型 | 行为 |
|------|------|------|
| query-input | `<textarea>` | 3 行，placeholder "请输入您的问题..." |
| send-btn | `<button>` | loading 时 disabled + 文案"发送中…" |
| clear-btn | `<button>` | 清空输入框 |

## 6. 错误处理

| 后端 `error.code` | HTTP | 前端展示 |
|--------------------|------|----------|
| `EMPTY_QUERY` | 400 | 错误提示："请输入问题" |
| `INVALID_QUERY` | 400 | 错误提示："问题过长" |
| `SESSION_NOT_FOUND` | 404 | 错误提示：“会话不存在”，自动刷新会话列表 |
| `INVALID_SESSION_ID` | 400 | 错误提示：“会话 ID 格式错误” |
| 网络异常 / 5xx | 500 | 错误提示：“服务异常，请稍后重试” |

## 7. 状态管理

> React `useState` Hooks，列出你的组件需要管理哪些状态。

| State 变量 | 类型 | 初始值 | 用途 |
|------------|------|--------|------|
| `sessions` | `Session[]` | `[]` | 会话列表 |
| `currentSession` | `Session\|null` | `null` | 当前选中会话 |
| `records` | `QARecord[]` | `[]` | 当前会话的问答记录列表 |
| `query` | `string` | `""` | 输入框当前内容 |
| `loading` | `boolean` | `false` | 是否正在等待问答响应 |
| `error` | `string\|null` | `null` | 当前错误提示信息 |
| `capabilities` | `Caps\|null` | `null` | 能力探测结果（CoPaw/百炼配置状态） |

## 8. US → FSD 对齐表

> 确保每条 US 的前端相关 AC 都在本文中有对应描述。

| UserStory | 验收标准涉及前端 | 本文章节 |
|-----------|------------------|----------|
| US-001 | 会话列表 + 删除 | 3 Sidebar |
| US-002 | query 非空 1~500 | 5 输入区 |
| US-003 | 历史问答记录展示 | 4 Main 内容区 C 状态 |
| US-004 | 能力状态芯片显示 | 2 Header |
| US-005 | 错误提示与反馈 | 6 错误处理 |

---

## 9. 研报上传区

> 对齐 `05` US-004。描述研报上传的 UI 规格与交互流程。

### 9.1 上传入口

| 元素 | 位置 | 说明 |
|------|------|------|
| 上传研报按钮 | Main 内容区工具栏 | 当前会话的工具栏中新增"上传研报"按钮 |

### 9.2 上传面板

点击按钮后弹出上传面板，支持以下交互：

| 特性 | 规格 |
|------|------|
| 触发方式 | 点击按钮弹出模态框 / 支持拖拽文件到面板区域 |
| 支持格式 | PDF（.pdf）、HTML（.html/.htm） |
| 文件大小限制 | ≤50MB |

### 9.3 上传流程

```
选择文件 → 显示上传进度条 → 上传完成自动触发解析 → 显示解析状态
```

| 阶段 | 界面表现 |
|------|----------|
| 文件选择 | 弹出文件选择器，过滤仅显示 .pdf/.html/.htm 文件 |
| 上传中 | 显示进度条（百分比），可取消上传 |
| 解析中 | 进度条完成，显示"解析中..."状态动画 |
| 解析完成 | 自动跳转到关键信息展示（§8），关闭上传面板 |
| 解析失败 | 显示错误提示，保留重新上传选项 |

### 9.4 错误处理

| 错误场景 | 提示文案 |
|----------|----------|
| 文件格式不支持 | "仅支持 PDF 和 HTML 格式" |
| 文件超过 50MB | "文件过大，请上传 50MB 以内的文件" |
| 解析失败 | "研报解析失败，请检查文件格式" |
| 网络异常 | "上传失败，请检查网络后重试" |

## 10. 关键信息展示

> 对齐 `05` US-004。描述研报解析结果的展示规格。

### 10.1 展示位置

- **位置**：Main 内容区，作为会话的一部分
- **形式**：信息卡片形式展示，多个研报以列表形式排列

### 10.2 信息卡片布局

```
┌─────────────────────────────────────────────┐
│  研报标题（文件名）                              │
│  发布机构 · 发布日期                             │
├─────────────────────────────────────────────┤
│  [买入]  [目标价: ¥188.00]  [当前价: ¥165.20]  │
├─────────────────────────────────────────────┤
│  核心观点：                                    │
│  1. 业绩超预期，Q3 营收同比增长 25%              │
│  2. 新产品线拓展顺利，市场份额持续提升            │
│  3. 管理层战略清晰，长期增长逻辑不变              │
├─────────────────────────────────────────────┤
│  [重新解析]  [删除]                            │
└─────────────────────────────────────────────┘
```

### 10.3 卡片元素规格

| 元素 | 规格 |
|------|------|
| **顶部信息** | 研报标题（文件名）+ 发布机构 + 发布日期 |
| **评级芯片** | 买入(绿色) / 增持(蓝色) / 中性(黄色) / 减持(橙色) / 卖出(红色) |
| **目标价卡片** | 突出显示目标价数值，如有当前价则显示对比（涨幅/跌幅） |
| **核心观点列表** | 编号列表展示，每条观点一行，最多显示 5 条 |
| **底部操作** | 重新解析按钮、删除按钮 |

### 10.4 评级芯片颜色映射

| 评级 | 颜色 | 色值参考 |
|------|------|----------|
| 买入 | 绿色 | bg-green-100 text-green-800 |
| 增持 | 蓝色 | bg-blue-100 text-blue-800 |
| 中性 | 黄色 | bg-yellow-100 text-yellow-800 |
| 减持 | 橙色 | bg-orange-100 text-orange-800 |
| 卖出 | 红色 | bg-red-100 text-red-800 |

### 10.5 研报列表

| 位置 | 说明 |
|------|------|
| Sidebar 区域 | 显示当前会话的所有已上传研报文件名列表 |
| Main 区域 | 点击 Sidebar 研报条目，Main 区域切换展示对应研报详情 |
| 当前选中 | 高亮显示当前正在查看的研报条目 |

## 11. 研报对比区

> 对齐 `05` US-005。描述研报对比功能的 UI 规格。

### 11.1 对比入口

| 条件 | 界面表现 |
|------|----------|
| 会话中已解析研报 ≥2 份 | 工具栏显示"对比研报"按钮 |
| 会话中已解析研报 <2 份 | 对比按钮禁用，hover 提示"请至少上传 2 份研报" |

### 11.2 研报选择器

点击"对比研报"按钮后弹出选择面板：

| 特性 | 规格 |
|------|------|
| 选择方式 | 勾选框多选 |
| 最少选择 | 2 份 |
| 最多选择 | 5 份 |
| 默认状态 | 全部未选中，实时显示已选数量（已选 X/5） |
| 确认按钮 | 选择 ≥2 份时可用，点击生成对比表 |

### 11.3 对比表格规格

| 维度 | 规格 |
|------|------|
| **列** | 每份研报一列，表头显示研报文件名 + 发布机构 |
| **行** | 评级、目标价、核心观点、发布日期 |
| **差异高亮** | 不同评级/目标价用不同背景色标注，相同值用默认背景 |

### 11.4 对比表格布局示例

```
┌─────────────┬──────────────┬──────────────┬──────────────┐
│   指标      │ 研报A_中金    │ 研报B_高盛    │ 研报C_摩根    │
├─────────────┼──────────────┼──────────────┼──────────────┤
│   评级      │   [买入]     │   [增持]     │   [买入]     │
│             │  bg-green    │  bg-blue     │  bg-green    │
├─────────────┼──────────────┼──────────────┼──────────────┤
│   目标价    │   ¥188.00    │   ¥175.00    │   ¥190.00    │
│             │  bg-yellow   │  bg-default  │  bg-yellow   │
├─────────────┼──────────────┼──────────────┼──────────────┤
│   发布日期  │  2026-04-10  │  2026-04-08  │  2026-04-12  │
├─────────────┼──────────────┼──────────────┼──────────────┤
│   核心观点  │ 1. 业绩超预期 │ 1. 稳健增长   │ 1. 高增长确定 │
│             │ 2. 市场份额提升│ 2. 估值合理   │ 2. 护城河深厚 │
└─────────────┴──────────────┴──────────────┴──────────────┘
```

### 11.5 加载与错误状态

| 状态 | 界面表现 |
|------|----------|
| 对比生成中 | 显示加载动画（旋转图标 + "正在生成对比表..."） |
| 选择不足 2 份 | 点击确认时提示"请至少选择 2 份研报进行对比" |
| 对比失败 | 提示"对比生成失败，请重试" |

## 12. 状态管理（扩展）

> 在 §7 基础上，增加研报相关状态。

| State 变量 | 类型 | 初始值 | 用途 |
|------------|------|--------|------|
| `reports` | `Report[]` | `[]` | 当前会话的研报列表 |
| `currentReport` | `Report\|null` | `null` | 当前选中的研报 |
| `uploadProgress` | `number` | `0` | 上传进度百分比 |
| `parsingStatus` | `'idle'\|'uploading'\|'parsing'\|'success'\|'error'` | `'idle'` | 研报解析状态 |
| `compareSelected` | `string[]` | `[]` | 对比模式已选中的研报 ID 列表 |
| `compareLoading` | `boolean` | `false` | 是否正在生成对比表 |
| `compareResult` | `CompareResult\|null` | `null` | 对比结果数据 |

## 8. US → FSD 对齐表（更新）

> 确保每条 US 的前端相关 AC 都在本文中有对应描述。

| UserStory | 验收标准涉及前端 | 本文章节 |
|-----------|------------------|----------|
| US-001 | 会话列表 + 删除 | 3 Sidebar |
| US-002 | query 非空 1~500 | 5 输入区 |
| US-003 | 历史问答记录展示 | 4 Main 内容区 C 状态 |
| US-004 | 能力状态芯片显示 | 2 Header |
| US-005 | 错误提示与反馈 | 6 错误处理 |
| US-004 | 研报上传与解析展示 | §9 研报上传区 · §10 关键信息展示 |
| US-005 | 研报对比 | §11 研报对比区 |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-04-14 | 首版填写 |
| v0.2 | 2026-04-14 | 追加研报相关功能规格（§9-§12） |
