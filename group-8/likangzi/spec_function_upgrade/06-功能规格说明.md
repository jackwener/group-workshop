# 06 — 功能规格说明（功能升级版）

> **06 只做一件事**：把 `05` 的 User Story 翻译成**前端可实现的界面行为规格**。

---

| 项 | 值 |
|---|---|
| 模块编号 | M2-RPT |
| 模块名称 | 研报分析增强 |
| 文档版本 | v1.0 |
| 阶段 | Spec（What — UI / UX 行为） |
| 上游 | `05` UserStory 与验收标准 |
| 下游 | → `08` 架构 · `09` API · `13` E2E 测试 |

---

## 13. 股票代码展示与详情弹窗

> 对齐 `05` US-006。描述股票代码的展示与交互规格。

### 13.1 股票代码标签

研报详情（信息卡片/列表行）中新增股票代码展示区域：

| 元素 | 规格 |
|------|------|
| **位置** | 研报详情卡片中，评级芯片右侧 |
| **样式** | 可点击标签，蓝色文字 + 下划线，hover 变色 |
| **内容** | 展示标准化后的股票代码（如 `SH600519`、`SZ000858`） |
| **多个代码** | 如提取到多个，横向排列，最多显示 3 个，超出显示 "+N" |

### 13.2 股票详情弹窗

点击股票代码标签后弹出模态框：

```
┌───────────────────────────────────────────────┐
│  ×  贵州茅台 (SH600519)                         │
├───────────────────────────────────────────────┤
│  当前价: ¥1,856.00  涨跌: +2.3%                 │
├───────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────┐  │
│  │  📈 近30日股价走势图（折线图）             │  │
│  │  [Mock数据: 30个日期+收盘价数据点]         │  │
│  └─────────────────────────────────────────┘  │
├───────────────────────────────────────────────┤
│  最近财报 (2025Q4)                              │
│  营收: 1,505亿  净利润: 862亿  同比: +15.2%     │
├───────────────────────────────────────────────┤
│  关键时点                                       │
│  • 2026-03-28  年报发布                          │
│  • 2026-04-10  股东大会                          │
│  • 2026-05-15  分红除权日                        │
└───────────────────────────────────────────────┘
```

| 区域 | 数据来源 | 说明 |
|------|----------|------|
| 头部 | `GET /stock/{code}/detail` → `name`, `code` | 公司名 + 代码 |
| 价格行 | `current_price`, `change_percent` | 当前价 + 涨跌幅 |
| 走势图 | `price_history[]` | 30 日收盘价数组，前端用 SVG/Canvas 渲染 |
| 财报区 | `financial_summary` | 最近一期营收、净利润、同比增速 |
| 时点区 | `key_events[]` | 关键时间节点列表 |

### 13.3 Mock 数据规格

所有股票数据均为 Mock，后端维护静态 JSON：

| 股票代码 | 公司名称 | 说明 |
|----------|----------|------|
| SH600519 | 贵州茅台 | 白酒龙头 |
| SZ000858 | 五粮液 | 白酒标杆 |
| SH688256 | 寒武纪 | AI 芯片 |
| 默认 | 模拟公司 | 未匹配代码返回默认 Mock |

## 14. 观点原文定位弹窗

> 对齐 `05` US-007。描述核心观点的原文引用展示。

### 14.1 触发入口

| 元素 | 规格 |
|------|------|
| **位置** | 每条核心观点文本右侧 |
| **样式** | 📍 图标按钮，hover 提示"查看原文" |
| **触发** | 点击图标 |

### 14.2 原文定位弹窗

```
┌─────────────────────────────────────────┐
│  ×  原文引用 — 贵州茅台-中信证券.pdf       │
├─────────────────────────────────────────┤
│                                         │
│  ……前文内容（灰色）……                    │
│  ██ 高亮标记的观点原文 ██                 │
│  ……后文内容（灰色）……                    │
│                                         │
├─────────────────────────────────────────┤
│  位置：第 1,256 字符处                    │
└─────────────────────────────────────────┘
```

| 元素 | 规格 |
|------|------|
| 上下文范围 | 观点位置前后各 200 字符 |
| 高亮样式 | 黄色背景 `bg-yellow-200` |
| 不可用状态 | position 为 null 时，图标置灰，hover 提示"原文定位不可用" |

## 15. 研报列表勾选交互（对比优化）

> 对齐 `05` US-008。描述研报列表形式的勾选对比交互。

### 15.1 列表布局

替换原有卡片式研报展示，改为**表格列表**形式：

```
┌──┬────────────────────┬──────┬────────┬──────────┬────────────┬──────┐
│☐ │ 文件名              │ 评级  │ 目标价  │ 股票代码   │ 上传时间    │ 操作  │
├──┼────────────────────┼──────┼────────┼──────────┼────────────┼──────┤
│☑ │ 贵州茅台-中信.pdf    │ 买入  │ ¥2100  │ SH600519  │ 04-14 10:30│ 🗑️   │
│☐ │ 寒武纪-招商.pdf      │ 增持  │ ¥85    │ SH688256  │ 04-14 11:00│ 🗑️   │
│☑ │ 茅台-国泰.pdf       │ 增持  │ ¥1950  │ SH600519  │ 04-15 09:00│ 🗑️   │
└──┴────────────────────┴──────┴────────┴──────────┴────────────┴──────┘
                                              已选 2/5  [对比已选(2)]
```

### 15.2 勾选规则

| 规则 | 说明 |
|------|------|
| 最少勾选 | 2 份，不足时"对比已选"按钮禁用 |
| 最多勾选 | 5 份，达上限后未选行复选框置灰 |
| 计数显示 | 底部实时显示"已选 N/5" |
| 全选/取消 | 表头提供全选复选框（最多选前 5 个） |

### 15.3 列表排序

| 默认排序 | 按上传时间倒序 |
|----------|--------------|
| 可排序列 | 文件名、评级、上传时间 |

## 16. 研报删除功能

> 对齐 `05` US-008 AC-008-04/05。

| 交互 | 说明 |
|------|------|
| 触发 | 点击列表行中的 🗑️ 删除按钮 |
| 确认弹窗 | "确认删除研报「{file_name}」？此操作不可恢复。" |
| 确认后 | 调用 `DELETE /reports/{id}`，从列表移除，Toast 提示"删除成功" |
| 取消 | 关闭弹窗，不做操作 |
| 副作用 | 如该研报正在对比结果中，对比结果自动关闭 |

## 17. 共同观点与差异观点展示

> 对齐 `05` US-009。描述对比结果中的智能观点分组。

### 17.1 展示位置

在对比表格**下方**，新增两个折叠面板：

```
┌─────────────────────────────────────────────┐
│  [维度对比表格 - 同现有]                       │
├─────────────────────────────────────────────┤
│  ▼ 共同观点 (3)                               │
│  ┌─────────────────────────────────────────┐│
│  │ 1. 业绩超预期，营收增速保持双位数          ││
│  │    📎 中信证券 · 国泰君安                  ││
│  │ 2. 高端白酒需求韧性强                     ││
│  │    📎 中信证券 · 国泰君安 · 高盛           ││
│  └─────────────────────────────────────────┘│
├─────────────────────────────────────────────┤
│  ▼ 差异观点 (4)                               │
│  ┌─────────────────────────────────────────┐│
│  │ 中信证券:                                 ││
│  │   • 新产品线拓展顺利                       ││
│  │ 国泰君安:                                 ││
│  │   • 估值偏高，建议等待回调                  ││
│  │ 高盛:                                     ││
│  │   • 海外市场拓展成关键增量                  ││
│  └─────────────────────────────────────────┘│
└─────────────────────────────────────────────┘
```

### 17.2 展示规格

| 元素 | 规格 |
|------|------|
| 共同观点 | 列表展示，每条标注涉及的研报（机构名） |
| 差异观点 | 按研报分组展示，每组标注机构名 |
| 折叠/展开 | 默认展开，可点击标题折叠 |
| 空状态 | "未发现共同观点" / "各研报观点无明显差异" |

## 18. 研报知识库页面

> 对齐 `05` US-010。描述全局研报知识库的页面规格。

### 18.1 入口

| 元素 | 规格 |
|------|------|
| **位置** | 主 Tab 栏，新增"知识库"Tab（位于"问答"和"研报分析"之后） |
| **图标** | 📚 书本图标 |

### 18.2 页面布局

```
┌─────────────────────────────────────────────┐
│  搜索: [________________] 筛选: [机构▼] [代码▼] │
├─────────────────────────────────────────────┤
│  ☐ 文件名    │ 评级 │ 目标价 │ 代码  │ 机构 │ 时间  │ 操作 │
│  ☑ 茅台-中信  │ 买入 │ ¥2100 │ 600519│ 中信 │ 04-14 │ 🗑️  │
│  ☐ 寒武纪-招商│ 增持 │ ¥85   │ 688256│ 招商 │ 04-15 │ 🗑️  │
│  ☑ 茅台-高盛  │ 买入 │ ¥2200 │ 600519│ 高盛 │ 04-15 │ 🗑️  │
├─────────────────────────────────────────────┤
│                     已选 2/5  [对比已选(2)]    │
└─────────────────────────────────────────────┘
```

### 18.3 搜索与筛选

| 功能 | 规格 |
|------|------|
| 搜索 | 按文件名模糊匹配，实时过滤（debounce 300ms） |
| 机构筛选 | 下拉单选，选项从已有研报的 institution 字段聚合 |
| 股票代码筛选 | 下拉单选，选项从已有研报的 stock_codes 字段聚合 |
| 重置 | 点击"重置"清空所有筛选条件 |

### 18.4 与会话级研报的关系

| 场景 | 行为 |
|------|------|
| 知识库展示 | 显示所有研报，不区分会话 |
| 知识库对比 | 可跨不同会话的研报进行对比 |
| 知识库删除 | 删除研报同时从关联会话中移除 |

## 19. 状态管理（扩展）

> 在基线 §7/§12 基础上，增加 V2 升级相关状态。

| State 变量 | 类型 | 初始值 | 用途 |
|------------|------|--------|------|
| `stockDetail` | `StockDetail\|null` | `null` | 当前查看的股票详情数据 |
| `stockDetailLoading` | `boolean` | `false` | 股票详情加载中 |
| `sourceTextModal` | `{text, position, fileName}\|null` | `null` | 原文定位弹窗数据 |
| `knowledgeReports` | `Report[]` | `[]` | 知识库研报列表 |
| `searchKeyword` | `string` | `""` | 知识库搜索关键词 |
| `filterInstitution` | `string\|null` | `null` | 机构筛选值 |
| `filterStockCode` | `string\|null` | `null` | 股票代码筛选值 |

## 20. US → FSD 对齐表

| UserStory | 验收标准涉及前端 | 本文章节 |
|-----------|------------------|----------|
| US-006 | 股票代码标签 + 详情弹窗 | §13 |
| US-007 | 观点原文定位弹窗 | §14 |
| US-008 | 列表勾选 + 删除 | §15, §16 |
| US-009 | 共同/差异观点展示 | §17 |
| US-010 | 知识库页面 | §18 |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-04-15 | 功能升级首版 |
