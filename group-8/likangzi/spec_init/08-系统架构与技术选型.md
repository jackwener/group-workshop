# 08 — 系统架构与技术选型（引导版模板）

> **08 只做一件事**：描述模块的**系统架构决策和技术选型理由**。"用户要什么"在 05/06；"约束底线"在 07；"系统怎么组织、为什么这样组织"在本文。

---

| 项 | 值 |
|---|---|
| 模块编号 | M1-QA |
| 模块名称 | 投研问答助手 |
| 文档版本 | v0.1 |
| 阶段 | Design（How — 架构决策） |
| 上游 | `05` 用户故事 · `06` 功能规格 · `07` 非功能需求 |
| 下游 | → `09` API 接口规格 · `10` 数据模型 · `12` 实施计划 |

---

## 1. 架构总览

### 1.1 系统上下文

```
┌─────────────────────────────────┐
│       用户浏览器 React SPA       │
│       http://localhost:5173     │
└──────────────┬──────────────────┘
               │ HTTP  /api/v1/agent/*
               ▼
┌─────────────────────────────────┐
│     Flask Backend (WSGI)        │
│     http://localhost:5000       │
│  ┌────────────────────────────┐ │
│  │  agent_bp — 6 个 API 端点  │ │
│  │    ↓          ↓         ↓  │ │
│  │ CoPawAgent  Storage  _ok() │ │
│  └────┬──────────┬────────────┘ │
│       ▼          ▼              │
│  ┌────────┐ ┌──────────┐       │
│  │ LLM 层 │ │ 文件系统  │       │
│  │CoPaw   │ │ ./data/  │       │
│  │百炼    │ │ *.json   │       │
│  │Demo    │ └──────────┘       │
│  └────────┘                    │
└─────────────────────────────────┘
```

### 1.2 架构风格

> 选择架构风格时思考：教学场景需要什么样的复杂度？分层带来什么好处？

| 决策 | 选型 | 理由 |
|------|------|------|
| **前后端分离** | React SPA + Flask REST API | 教学清晰度：前端独立开发服务器 |
| **单体部署** | 前后端各一个进程 | 不引入微服务复杂度 |
| **分层架构** | Route → Agent → Storage | 替换存储引擎时上层零改动 |
| **三级降级编排** | CoPaw → 百炼 → Demo 静默降级 | 外部 LLM 不可用时系统仍可离线运行，满足 03 §5 风险应对 |

## 2. 后端分层结构

> 每层写清"允许做什么、禁止做什么"。

| 层 | 文件 | 允许做 | 禁止做 |
|----|------|--------|--------|
| **Route** | `agent_bp.py` | 参数校验、HTTP 状态码、traceId | 直接读写文件、调用 LLM |
| **Agent** | `agent.py` | 降级编排、结果组装 | 感知 HTTP 请求/响应 |
| **Provider** | `copaw_bridge.py` / `bailian_qa.py` | HTTP 调用外部 API | 操作本地存储 |
| **Storage** | `storage.py` | 文件 CRUD、数据校验 | 调用外部 API |

## 3. 前端架构

| 项 | 选型 | 理由 |
|----|------|------|
| UI 框架 | React（Hooks）≥ 18 | 教学通用性最高 |
| 构建工具 | Vite ≥ 5 | 极快 HMR，零配置启动 |
| 状态管理 | `useState` + `useEffect` | 8 个 state 变量足够 |
| HTTP 客户端 | `fetch` API | 浏览器原生，不引入 axios |
| 样式方案 | 原生 CSS（内联 / CSS Modules） | 不引入 Tailwind 等额外构建依赖，降低学员认知负担 |

## 4. 三级降级编排

> 对齐 `07` 1.2 降级策略。画出降级链路，标明每级的 `answer_source` 和 `llm_used`。

```
ask(query, session_id)
  ├─[1] CoPaw  → {source:"copaw",  llm:true}
  ├─[2] 百炼   → {source:"bailian",llm:true}
  └─[3] Demo   → {source:"demo",   llm:false}
```

| 提供商 | 文件 | 配置检测 | 超时 | 错误处理 |
|--------|------|----------|------|----------|
| **CoPaw** | `copaw_bridge.py` | `IRA_COPAW_*_URL` 非空 | 20s | 返回 None 静默降级 |
| **百炼** | `bailian_qa.py` | `DASHSCOPE_API_KEY` 非空 | 120s | 区分多类错误码 |
| **Demo** | `agent.py` | 始终可用 | 0s | 纯字符串拼接 |

## 5. 数据存储架构

### 5.1 存储演进路线

| 阶段 | 存储 | 变更范围 | 说明 |
|------|------|----------|------|
| **P0（当前）** | JSON 文件 | — | 适合教学 |
| P1 | SQLite | 仅替换 Storage 实现层 | 结构化查询 + 事务支持，仍为单文件部署 |
| P2 | PostgreSQL | Storage 层 + 部署配置 | 生产级并发读写、索引优化、备份恢复 |

## 6. 部署架构

| 服务 | 端口 | 启动命令 |
|------|------|----------|
| Flask Backend | **5000** | `python -m flask --app wsgi run --port 5000` |
| Vite Frontend | **5173** | `npm run dev` |

## 7. ADR（架构决策记录）

> 每条 ADR 记录一个"为什么选 A 而不选 B"的决策。至少写 2 条。

**示例 — ADR-001**：为什么选 Flask 而非 FastAPI？Flask 学习曲线低，无需 async/await，学员 Python 基础即可上手。

| ADR | 决策 | 核心理由 |
|-----|------|----------|
| ADR-001 | Flask 而非 FastAPI | 学习曲线低，教学更适合 |
| ADR-002 | JSON 文件而非 SQLite | 零依赖、可读性高，学员可直接查看数据文件调试；分层架构保证未来可平滑替换 |
| ADR-003 | 前端单文件 App 而非多组件拆分 | 教学版功能简单（侧边栏 + 主内容区 + 输入区），单文件便于学员通读全貌，降低上手难度 |

## 8. 安全架构

> 对齐 `07` 4 安全需求。当前教学版与生产需求的差距。

| 层 | 当前措施 | 生产改进 |
|----|----------|----------|
| 认证 | 无（开放访问） | JWT |
| CORS | 全开放 | 白名单 |
| 密钥 | 环境变量 `.env` | Vault / Secrets Manager |
| 输入校验 | Route 层白名单校验 query/session_id | WAF + 服务端双重校验 |
| 日志审计 | traceId 链路追踪 | ELK 集中日志 + 异常告警 |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-04-14 | 首版填写 |

## 9. 研报处理架构扩展

在现有四层（Route/Agent/Provider/Storage）基础上，研报功能新增 ReportParser 模块：

| 模块 | 文件 | 职责 | 输入 | 输出 |
|------|------|------|------|------|
| ReportParser | `report_parser.py` | PDF/HTML 研报解析，提取关键信息 | 文件路径 | 结构化提取结果 |

**研报处理流程**：
```
用户上传文件 → Route(agent_bp.py) 接收 multipart/form-data
  → Storage(storage.py) 保存文件到 data/reports/
  → ReportParser(report_parser.py) 解析文件
    ├─ PDF: pdfplumber 提取文本 → LLM 提取结构化信息
    ├─ HTML: BeautifulSoup 提取文本 → LLM 提取结构化信息
    └─ 失败: 返回 PARSE_FAILED 错误
  → Storage 保存提取结果到 reports.json
  → Route 返回 report_id + extracted_data
```

**研报对比流程**：
```
用户选择多份研报 → Route 接收 report_ids[]
  → Storage 读取各研报的 extracted_data
  → 生成对比表（直接字段比较，无需 LLM）
  → Route 返回 comparison_table
```

### 9.1 ADR-004 PDF 解析库选择

| 选项 | 优势 | 劣势 | 决策 |
|------|------|------|------|
| pdfplumber | 精确提取文本和表格，开源免费 | 复杂排版可能丢失格式 | **选用** |
| PyPDF2 | 轻量 | 表格提取能力差 | 排除 |
| paddleocr | OCR 能力强 | 依赖重，需 GPU | 排除（教学版不适合） |

**理由**：教学版优先选择轻量依赖，pdfplumber 对文本型 PDF 效果好，满足 P0 需求。

### 9.2 研报存储方案

| 项 | 规格 |
|----|------|
| 研报元数据 | `{DATA_DIR}/reports.json`（JSON 文件，与 sessions.json 同级） |
| 研报原始文件 | `{DATA_DIR}/reports/` 目录，以 `{report_id}.{ext}` 命名 |
| 编码 | UTF-8，缩进 2 空格（与 sessions.json 一致） |
| 读写模式 | RMW（与现有模式一致） |
