# 13 — 测试策略与质量门禁

---

| 项       | 值                                                 |
| -------- | -------------------------------------------------- |
| 模块编号 | M2-Review                                          |
| 模块名称 | 研报审核助手                                       |
| 文档版本 | v0.1                                               |
| 阶段     | Test（断言 — 怎样算通过）                          |
| 追溯文档 | `05` UserStory · `09` API 接口规格 · `14` 追踪矩阵 |

---

## 一、测试分层

> 共 3 层，写测试前先搞清楚每层验什么。

| 层级               | 工具                       | 覆盖对象                                          | 说明                               |
| ------------------ | -------------------------- | ------------------------------------------------- | ---------------------------------- |
| **L1 Unit**        | pytest                     | 单个函数/类的逻辑（审核引擎、评分计算、文件解析） | 不依赖网络，最快                   |
| **L2 Integration** | pytest + Flask test_client | API 端点 + 数据库 + 业务逻辑                      | 模拟 HTTP 请求，覆盖全部 10 个端点 |
| **L3 E2E**         | 手动测试                   | 完整用户旅程（浏览器操作）                        | 当前无自动化 E2E，人工执行         |

> **注**：当前项目无 Contract 层（L3 schema 断言）和自动化 E2E（Playwright/Cypress），手动 E2E 测试覆盖完整用户旅程。

## 二、用例拆分原则

> **核心理念：数"可独立失败的点"** — 每条 TC 只回答一个问题，失败时不看代码就知道哪里坏了。

| 来源     | 计数方法                        | 示例                             |
| -------- | ------------------------------- | -------------------------------- |
| 正常路径 | 每个端点的 happy path = 1 条    | `POST /reviews → 201` = TC-001   |
| 错误码   | 每个 distinct error code = 1 条 | `UNSUPPORTED_FILE_TYPE` = TC-005 |
| 分支路径 | 每个 if/else 降级分支 = 1 条    | AI 不可用 → Mock 降级 = TC-008   |
| 筛选参数 | 每组筛选条件 = 1 条             | `status=failed` 筛选 = TC-014    |

## 三、测试用例清单

### 3.1 仪表盘统计 API 测试

> 覆盖 `GET /api/v1/dashboard/stats`、`GET /api/v1/dashboard/trend`、`GET /api/v1/dashboard/top-issues`。对齐 US-005（AC-005-01~03）。

| TC-ID       | 层级        | 断言要点                                                                                        | 关联 REQ     | 优先级 |
| ----------- | ----------- | ----------------------------------------------------------------------------------------------- | ------------ | ------ |
| TC-M2RV-001 | Integration | `GET /dashboard/stats` → 200，含 totalReviews / passRate / avgScore / todayCount / pendingCount | REQ-M2RV-005 | P0     |
| TC-M2RV-002 | Integration | `GET /dashboard/trend` → 200，返回 trend 数组（7 个元素），每项含 day / total / passed / failed | REQ-M2RV-005 | P0     |
| TC-M2RV-003 | Integration | `GET /dashboard/top-issues` → 200，返回 issues 数组（最多 5 个），每项含 name / count / pct     | REQ-M2RV-005 | P0     |

### 3.2 提交审核流程测试

> 覆盖 `POST /api/v1/reviews`。对齐 US-001（AC-001-01~03）、US-008（AC-008-01~03）。

| TC-ID       | 层级        | 断言要点                                                                                                      | 关联 REQ     | 优先级 |
| ----------- | ----------- | ------------------------------------------------------------------------------------------------------------- | ------------ | ------ |
| TC-M2RV-004 | Integration | `POST /reviews` 合法 PDF + mode=rule → 201，返回 id（格式 `RV-{年份}-{4位序号}`）/ status=`pending` / message | REQ-M2RV-001 | P0     |
| TC-M2RV-005 | Integration | `POST /reviews` 不支持格式（.xlsx）→ 400，error.code=`UNSUPPORTED_FILE_TYPE`                                  | REQ-M2RV-001 | P0     |
| TC-M2RV-006 | Integration | `POST /reviews` 文件超过 50MB → 400，error.code=`FILE_TOO_LARGE`                                              | REQ-M2RV-001 | P0     |
| TC-M2RV-007 | Integration | `POST /reviews` 未提供文件 → 400，error.code=`INVALID_REQUEST`                                                | REQ-M2RV-001 | P0     |
| TC-M2RV-008 | Integration | `POST /reviews` mode=combined → 201，审核完成后同时包含规则引擎和 AI 引擎检出的问题                           | REQ-M2RV-008 | P0     |

### 3.3 审核状态查询测试

> 覆盖 `GET /api/v1/reviews/{id}/status`。对齐 US-002（AC-002-01~02）。

| TC-ID       | 层级        | 断言要点                                                                          | 关联 REQ     | 优先级 |
| ----------- | ----------- | --------------------------------------------------------------------------------- | ------------ | ------ |
| TC-M2RV-009 | Integration | `GET /reviews/{id}/status` → 200，含 id / status / progress / currentStep / steps | REQ-M2RV-002 | P0     |
| TC-M2RV-010 | Integration | `GET /reviews/{不存在id}/status` → 404，error.code=`REVIEW_NOT_FOUND`             | REQ-M2RV-002 | P0     |

### 3.4 审核报告获取测试

> 覆盖 `GET /api/v1/reviews/{id}`。对齐 US-003（AC-003-01~03）。

| TC-ID       | 层级        | 断言要点                                                                                                                                                                                   | 关联 REQ     | 优先级 |
| ----------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------ | ------ |
| TC-M2RV-011 | Integration | `GET /reviews/{id}` → 200，含 id / title / author / reportType / mode / status / score / issues[]，每个 issue 含 ruleId / ruleName / category / severity / location / excerpt / suggestion | REQ-M2RV-003 | P0     |
| TC-M2RV-012 | Integration | `GET /reviews/{不存在id}` → 404，error.code=`REVIEW_NOT_FOUND`                                                                                                                             | REQ-M2RV-003 | P0     |

### 3.5 审核历史列表测试

> 覆盖 `GET /api/v1/reviews`。对齐 US-004（AC-004-01~03）。

| TC-ID       | 层级        | 断言要点                                                                              | 关联 REQ     | 优先级 |
| ----------- | ----------- | ------------------------------------------------------------------------------------- | ------------ | ------ |
| TC-M2RV-013 | Integration | `GET /reviews` 默认分页 → 200，含 total / page / pageSize / list，按 submittedAt 倒序 | REQ-M2RV-004 | P0     |
| TC-M2RV-014 | Integration | `GET /reviews?status=failed&mode=rule` 筛选 → 200，仅返回匹配记录                     | REQ-M2RV-004 | P1     |
| TC-M2RV-015 | Integration | `GET /reviews?search=关键词` 模糊搜索 → 200，匹配 title / author / id                 | REQ-M2RV-004 | P1     |

### 3.6 报告导出测试

> 覆盖 `GET /api/v1/reviews/{id}/export`。对齐 US-007（AC-007-01~02）。

| TC-ID       | 层级        | 断言要点                                                                                               | 关联 REQ     | 优先级 |
| ----------- | ----------- | ------------------------------------------------------------------------------------------------------ | ------------ | ------ |
| TC-M2RV-016 | Integration | `GET /reviews/{id}/export?format=pdf` → 200，Content-Type 为 `application/pdf` 或 `text/plain`（降级） | REQ-M2RV-007 | P2     |
| TC-M2RV-017 | Integration | `GET /reviews/{id}/export?format=docx` → 200，Content-Disposition 含文件名                             | REQ-M2RV-007 | P2     |

### 3.7 规则管理测试

> 覆盖 `GET /api/v1/rules` 和 `PATCH /api/v1/rules/{id}`。对齐 US-006（AC-006-01~03）。

| TC-ID       | 层级        | 断言要点                                                                                                         | 关联 REQ     | 优先级 |
| ----------- | ----------- | ---------------------------------------------------------------------------------------------------------------- | ------------ | ------ |
| TC-M2RV-018 | Integration | `GET /rules` → 200，返回 rules 数组（8 条内置规则），每条含 id / name / category / severity / mode / enabled     | REQ-M2RV-006 | P0     |
| TC-M2RV-019 | Integration | `GET /rules?category=compliance` 筛选 → 200，仅返回 compliance 类别规则                                          | REQ-M2RV-006 | P1     |
| TC-M2RV-020 | Integration | `PATCH /rules/{id}` body `{enabled: false}` → 200，返回 enabled=false；再 `PATCH {enabled: true}` → enabled=true | REQ-M2RV-006 | P0     |

### 3.8 异常场景测试

> 覆盖边界条件和异常场景。

| TC-ID       | 层级        | 断言要点                                                                                      | 关联 REQ     | 优先级 |
| ----------- | ----------- | --------------------------------------------------------------------------------------------- | ------------ | ------ |
| TC-M2RV-021 | Integration | `GET /reviews/{不存在id}` → 404，error.code=`REVIEW_NOT_FOUND`，details 含 id                 | —            | P0     |
| TC-M2RV-022 | Integration | `POST /reviews` 上传空文件 → 400，error.code=`INVALID_REQUEST`                                | REQ-M2RV-001 | P1     |
| TC-M2RV-023 | Integration | `PATCH /rules/{不存在id}` → 404，error.code=`RULE_NOT_FOUND`                                  | REQ-M2RV-006 | P1     |
| TC-M2RV-024 | Integration | `PATCH /rules/{id}` body 缺少 enabled 或 enabled 非布尔值 → 400，error.code=`INVALID_REQUEST` | REQ-M2RV-006 | P1     |

## 四、质量门禁

| Gate-ID    | 条件                                    | 阻塞合并      |
| ---------- | --------------------------------------- | ------------- |
| **G-LINT** | `flake8` 通过（0 error）                | **是**        |
| **G-UNIT** | Unit 测试全绿                           | **是**        |
| **G-INT**  | Integration 测试全绿                    | **是**        |
| **G-COV**  | 单测覆盖率 ≥ 70%（服务层 + 模型层）     | **是**        |
| **G-AC**   | P0 验收标准全覆盖（US-001/002/003/008） | **是**        |
| **G-SEC**  | 安全扫描 0 Critical（`pip audit`）      | **是**        |
| **G-PERF** | API P95 响应 < 2000ms，规则审核 < 30s   | 否（warning） |

### 执行命令

```bash
# 后端测试
cd group-8/team1/report-compliance-audit/backend
python -m pytest tests/ -q

# 后端测试 + 覆盖率
python -m pytest tests/ -q --cov=app --cov-report=term-missing

# Lint 检查
flake8 app/ --max-line-length=120

# 安全扫描
pip audit
```

## 五、TDD 适用场景

| 场景                                | TDD 价值 | 建议             |
| ----------------------------------- | -------- | ---------------- |
| 简单 CRUD（规则列表、历史查询）     | 低       | 写完补测试       |
| 参数校验 / 错误码（文件类型、大小） | 中       | 建议 TDD         |
| 评分计算逻辑（P0/P1/P2 扣分）       | **高**   | **强烈建议 TDD** |
| 审核引擎（规则匹配、AI 降级）       | **高**   | **强烈建议 TDD** |
| 文件解析（PDF/DOCX 提取标题作者）   | **高**   | **建议 TDD**     |

## 六、追溯关系

> 汇总所有 TC 与 REQ 的映射，完成后同步到 `14` 追踪矩阵。

| 需求编号     | 已规划 TC                        | 说明                                                              |
| ------------ | -------------------------------- | ----------------------------------------------------------------- |
| REQ-M2RV-001 | TC-M2RV-004~008, TC-M2RV-022     | 提交审核：正常 + 格式错误 + 大小超限 + 无文件 + 空文件 + 联合模式 |
| REQ-M2RV-002 | TC-M2RV-009~010                  | 审核进度：正常查询 + 不存在记录                                   |
| REQ-M2RV-003 | TC-M2RV-011~012                  | 审核报告：正常获取 + 不存在记录                                   |
| REQ-M2RV-004 | TC-M2RV-013~015                  | 审核历史：默认分页 + 多条件筛选 + 模糊搜索                        |
| REQ-M2RV-005 | TC-M2RV-001~003                  | 仪表盘：stats + trend + top-issues                                |
| REQ-M2RV-006 | TC-M2RV-018~020, TC-M2RV-023~024 | 规则管理：列表 + 筛选 + 启用禁用 + 异常                           |
| REQ-M2RV-007 | TC-M2RV-016~017                  | 报告导出：PDF + DOCX                                              |
| REQ-M2RV-008 | TC-M2RV-004, TC-M2RV-008         | 审核模式：规则模式 + 联合模式                                     |

---

| 版本 | 日期       | 说明     |
| ---- | ---------- | -------- |
| v0.1 | 2026-04-15 | 首版填写 |
