# 13 — 测试策略与质量门禁（引导版模板）

---

| 项 | 值 |
|---|---|
| 模块编号 | M1-QA |
| 模块名称 | 投研问答助手 |
| 文档版本 | v0.1 |
| 阶段 | Test（断言 — 怎样算通过） |
| 追溯文档 | `05` UserStory · `09` API 接口规格 · `14` 追踪矩阵 |

---

## 一、测试分层

> 共 4 层，写测试前先搞清楚每层验什么。

| 层级 | 工具 | 覆盖对象 | 说明 |
|------|------|----------|------|
| **L1 Unit** | pytest | 单个函数/类的逻辑 | 不依赖网络，最快 |
| **L2 Integration** | pytest + Flask test_client | API 端点 + 存储 | 模拟 HTTP 请求 |
| **L3 Contract** | pytest + schema 断言 | 响应体结构与 `09` 一致 | 验字段名、类型 |
| **L4 E2E** | Playwright / Cypress | 完整用户旅程 | 浏览器级操作 |

## 二、用例拆分原则

> **核心理念：数"可独立失败的点"** — 每条 TC 只回答一个问题，失败时不看代码就知道哪里坏了。

| 来源 | 计数方法 | 示例 |
|------|----------|------|
| 正常路径 | 每个端点的 happy path = 1 条 | `POST /ask → 200` = TC-001 |
| 错误码 | 每个 distinct error code = 1 条 | `EMPTY_QUERY` = TC-002 |
| 分支路径 | 每个 if/else 降级分支 = 1 条 | 无 API Key → demo = TC-004 |
| 类型约束 | Spec `09` 约定的字段类型 = 1 条 | `llm_used` 必须 bool = TC-006 |

## 三、★ 示例：已有测试用例

| TC-ID | 层级 | 测试函数 | 断言要点 | 关联 REQ |
|-------|------|----------|----------|----------|
| TC-M01-001 | Integration | `test_ask_success` | `POST /ask` → 200, 含 answer、llm_used、traceId | REQ-001 |
| TC-M01-002 | Integration | `test_ask_empty_query` | 空 query → 400, `EMPTY_QUERY` | REQ-005 |
| TC-M01-003 | Integration | `test_ask_long_query` | query >500 → 400, `INVALID_QUERY` | REQ-005 |
| TC-M01-004 | Integration | `test_bailian_fallback` | 无 API Key → `answer_source='demo'` | REQ-003 |

### 3.1 会话管理 API 测试

> 覆盖 GET /sessions、POST /sessions、DELETE /sessions/<id>。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-020 | Integration | GET /sessions → 200，返回 sessions 数组，含 traceId，按 updated_at 降序 | REQ-M1-006 | P0 |
| TC-M01-021 | Integration | POST /sessions → 201，含 session_id（UUID）、title、created_at、query_count=0 | REQ-M1-006 | P0 |
| TC-M01-022 | Integration | POST /sessions title 超 100 字符 → 400 `INVALID_TITLE` | REQ-M1-006 | P0 |
| TC-M01-023 | Integration | DELETE /sessions/<id> → 200，含 message、deleted_session_id，级联删除记录 | REQ-M1-006 | P0 |
| TC-M01-024 | Integration | DELETE /sessions/<invalid_id> → 400 `INVALID_SESSION_ID` | REQ-M1-006 | P0 |
| TC-M01-025 | Integration | DELETE /sessions/<nonexistent_id> → 404 `SESSION_NOT_FOUND` | REQ-M1-006 | P0 |

### 3.2 问答记录 API 测试

> 覆盖 GET /sessions/<id>/records。有记录和无记录两种场景。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-030 | Integration | GET /sessions/<id>/records → 200，返回 records 数组，每条含 record_id、query、answer、answer_source、timestamp | REQ-M1-007 | P0 |
| TC-M01-031 | Integration | GET /sessions/<id>/records 无记录时 → 200，返回空数组 records=[] | REQ-M1-007 | P1 |
| TC-M01-032 | Integration | GET /sessions/<nonexistent_id>/records → 404 `SESSION_NOT_FOUND` | REQ-M1-007 | P0 |
| TC-M01-033 | Integration | GET /sessions/<invalid_id>/records → 400 `INVALID_SESSION_ID` | REQ-M1-007 | P0 |

### 3.3 文件上传 API 测试

> 覆盖 POST /upload。正常上传、超限、格式错误等场景。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-048 | Integration | POST /upload PDF 文件 → 200，含 file_id、filename、file_type='pdf'、processing_status | REQ-M1-008 | P0 |
| TC-M01-049 | Integration | POST /upload 文件超 10MB → 400 `FILE_TOO_LARGE` | REQ-M1-008 | P0 |
| TC-M01-048a | Integration | POST /upload 不支持的格式（.txt）→ 400 `UNSUPPORTED_FORMAT` | REQ-M1-008 | P0 |
| TC-M01-048b | Integration | POST /upload session_id 不存在 → 404 `SESSION_NOT_FOUND` | REQ-M1-008 | P0 |

### 3.4 研报对比查询 API 测试

> 覆盖 GET /research/comparison。正常查询、分页、参数错误场景。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-050 | Integration | GET /research/comparison → 200，含 total、page、page_size、reports 数组 | REQ-M1-009 | P0 |
| TC-M01-050a | Integration | GET /research/comparison?company=中芯 → 200，模糊匹配返回匹配结果 | REQ-M1-009 | P0 |
| TC-M01-051 | Integration | GET /research/comparison?page=0 → 400 `INVALID_PAGE` | REQ-M1-009 | P0 |
| TC-M01-051a | Integration | GET /research/comparison?page_size=200 → 400 `INVALID_PAGE_SIZE` | REQ-M1-009 | P0 |

### 3.5 健康检查 API 测试

> 覆盖 GET /health 和 GET /capabilities。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-052 | Integration | GET /health → 200，含 status、timestamp、components、uptime_seconds | REQ-M1-010 | P1 |
| TC-M01-053 | Integration | GET /health components 含 llm_service、file_storage、database，各含 status 字段 | REQ-M1-010 | P1 |
| TC-M01-054 | Integration | GET /capabilities → 200，返回当前可用的 LLM 提供商信息 | REQ-M1-011 | P0 |
| TC-M01-055 | Contract | GET /health 响应体结构与 09 §10 定义一致，status 枚举 healthy/degraded/unhealthy | REQ-M1-010 | P1 |

## 四、质量门禁

| Gate-ID | 条件 | 阻塞合并 |
|---------|------|----------|
| **G-LINT** | `black --check` + `flake8` 通过 | **是** |
| **G-UNIT** | Unit 测试全绿 | **是** |
| **G-INT** | Integration 测试全绿 | **是** |
| **G-AC** | P0 验收标准全覆盖 | **是** |
| **G-PERF** | `response_time_ms < 5000` | 否（warning） |

### 执行命令

```bash
# 后端测试
cd groups/Group-X/NN/backend
python -m pytest tests/ -q

# 后端测试 + 覆盖率
python -m pytest tests/ -q --cov=. --cov-report=term-missing
```

## 五、TDD 适用场景

| 场景 | TDD 价值 | 建议 |
|------|----------|------|
| 简单 CRUD（列表、删除） | 低 | 写完补测试 |
| 参数校验 / 错误码 | 中 | 建议 TDD |
| 多级降级 / 状态机 | **高** | **强烈建议 TDD** |
| 金融计算 / 风险判定 | **极高** | **必须 TDD** |

## 六、追溯关系

> 汇总所有 TC 与 REQ 的映射，完成后同步到 `14` 追踪矩阵。

| 需求编号 | 已实现 TC | 待补充 TC |
|----------|-----------|------------|
| REQ-M1-001 | TC-M01-001 | — |
| REQ-M1-002 | TC-M01-001 | — |
| REQ-M1-003 | TC-M01-004 | — |
| REQ-M1-005 | TC-M01-002, 003 | — |
| REQ-M1-006 | — | TC-M01-020～025 |
| REQ-M1-007 | — | TC-M01-030～033 |
| REQ-M1-008 | — | TC-M01-048, 049, 048a, 048b |
| REQ-M1-009 | — | TC-M01-050, 050a, 051, 051a |
| REQ-M1-010 | — | TC-M01-052～053, 055 |
| REQ-M1-011 | TC-M01-005 | TC-M01-054 |
| 规则-005 | TC-M01-006 | — |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 【日期】 | 首版填写 |
