# 15 — 开发任务清单（Task List）

---

| 项 | 值 |
|---|---|
| 模块编号 | M1-QA |
| 模块名称 | 投研问答助手 |
| 文档版本 | v0.1 |
| 阶段 | Execution（开发执行） |
| 上游 | `05` UserStory · `08` 架构 · `09` API · `10` 数据 · `12` 实施计划 · `13` 测试策略 |

---

> **本文基于 `12` 里程碑（S1～S4）和 `WBS` 任务拆分，展开为可直接执行的开发任务。每条 Task 关联到具体的 API 端点、数据模型、测试用例，做到"拿到就能写"。**

## 任务状态说明

| 状态 | 含义 |
|------|------|
| ⬜ TODO | 未开始 |
| 🔵 IN PROGRESS | 开发中 |
| ✅ DONE | 已完成并通过测试 |
| ❌ BLOCKED | 被阻塞（标注阻塞原因） |

---

## S1 — 会话管理（REQ-001 / US-001）

> **交付目标**：会话 CRUD API + 前端侧栏，可演示新建/列表/删除会话
> **验证**：TC-M01-020～025 全绿
> **前置条件**：后端与前端可独立启动、演示数据目录可读写

### 后端任务

| Task-ID | 任务名称 | 详细描述 | 关联 Spec | DoD（完成标准） | 状态 |
|---------|----------|----------|-----------|----------------|------|
| T-S1-B01 | 初始化 Flask 项目结构 | 创建 `app.py`（Flask 入口）、`blueprints/agent_bp.py`（路由注册）、`storage.py`（存储层）、`data/` 目录；配置 CORS 白名单、UTF-8 编码 | `08` §2, §6 | Flask 启动成功，`/api/v1/agent/` 返回 404（无端点） | ⬜ |
| T-S1-B02 | 实现 Storage 层 — Session CRUD | `storage.py` 实现 `create_session()`、`get_sessions()`、`delete_session()`、`update_session_title()`；JSON 文件读写（RMW 模式），sessions 按 `updated_at` 降序 | `10` §3, §7.1 | 单元测试通过：创建/查询/删除/排序均符合预期 | ⬜ |
| T-S1-B03 | 实现 POST /sessions 端点 | `agent_bp.py` 注册路由，接收可选 `title`（默认"新会话"），校验 title ≤100 字符，生成 UUID session_id，返回 201 + traceId | `09` §4 | TC-M01-021, 022 通过 | ⬜ |
| T-S1-B04 | 实现 GET /sessions 端点 | 返回 sessions 数组，按 `updated_at` 降序，包含 session_id/title/created_at/updated_at/query_count | `09` §5 | TC-M01-020 通过 | ⬜ |
| T-S1-B05 | 实现 DELETE /sessions/\<id\> 端点 | 校验 session_id 格式（UUID），不存在返回 404 `SESSION_NOT_FOUND`，成功返回 200 + deleted_session_id；**级联删除**关联的 QARecord 和 UploadFile | `09` §6 | TC-M01-023, 024, 025 通过 | ⬜ |
| T-S1-B06 | 实现统一响应格式 | 所有成功响应包含 `traceId`（`tr_{uuid.hex}`）；错误响应遵循 `{error: {code, message, details, traceId}}` 格式；优先复用请求头 `X-Trace-Id` | `09` §2, `07` §3.1 | 所有端点响应包含 traceId，错误码格式一致 | ⬜ |
| T-S1-B07 | 会话管理集成测试 | 编写 pytest 用例覆盖 TC-M01-020～025，使用 Flask test_client，测试数据隔离到 `test_data/` | `13` §3.1 | 6 条测试用例全绿 | ⬜ |

### 前端任务

| Task-ID | 任务名称 | 详细描述 | 关联 Spec | DoD（完成标准） | 状态 |
|---------|----------|----------|-----------|----------------|------|
| T-S1-F01 | 初始化 React + Vite 项目 | `npm create vite@latest`，配置 React Router v6、CSS Modules、API 代理（proxy 到 localhost:5000）| `08` §3 | `npm run dev` 启动成功，页面空白可访问 | ⬜ |
| T-S1-F02 | 实现页面整体布局 | Header + Sidebar + Main Content 三栏布局；Header 显示标题"投研问答助手" | `06` §1 | 页面布局与 06 §1 ASCII 示意一致 | ⬜ |
| T-S1-F03 | 实现 Sidebar 会话列表 | 页面加载时调用 `GET /sessions`，按 `updated_at` 降序渲染列表；点击选中高亮 | `06` §3 | 会话列表正确渲染，选中态切换正常 | ⬜ |
| T-S1-F04 | 实现新建会话功能 | 点击"+ 新建"按钮调用 `POST /sessions`，新会话插入列表头部并自动选中，主内容区清空 | `06` §3 | 新建会话后列表更新，自动选中 | ⬜ |
| T-S1-F05 | 实现删除会话功能 | 右键菜单显示"删除"选项，二次确认弹窗，确认后调用 `DELETE /sessions/<id>`，从列表移除 | `06` §3, `05` AC-001-03 | 删除后列表更新，主内容区清空 | ⬜ |
| T-S1-F06 | 实现 Main 空状态（A 状态） | 无选中会话时，主内容区居中显示一个 "+ 新建会话" 按钮，点击后调用 `POST /sessions` 创建会话并自动切换到常见问题页面（B 状态） | `06` §4 状态 A | 空状态只显示新建按钮，点击后进入常见问题页 | ⬜ |
| T-S1-F07 | 实现 API 客户端封装 | `api.js` 封装 fetch 请求：统一 Base URL、错误处理、JSON 解析；导出 `getSessions()`、`createSession()`、`deleteSession()` | `08` §3 | API 调用正常，错误码正确捕获 | ⬜ |

---

## S2 — 问答核心（REQ-002 部分 + REQ-005 / US-002）

> **交付目标**：问答提交 API + 三级降级 + 前端对话区，可演示提问→获得回答→查看历史
> **验证**：TC-M01-001～004, 005, 030～031 全绿
> **前置条件**：S1 会话管理完成

### 后端任务

| Task-ID | 任务名称 | 详细描述 | 关联 Spec | DoD（完成标准） | 状态 |
|---------|----------|----------|-----------|----------------|------|
| T-S2-B01 | 实现 CoPaw 桥接层 | `copaw_bridge.py` 实现 CoPaw HTTP 调用，配置检测 `IRA_COPAW_*_URL`，超时 3s，失败返回 None 静默降级 | `08` §4 | 配置存在时调用成功，缺失时返回 None | ⬜ |
| T-S2-B02 | 实现百炼集成层 | `bailian_qa.py` 实现 DashScope API 调用，配置检测 `DASHSCOPE_API_KEY`，超时 5s，区分多类错误码 | `08` §4 | 配置存在时调用成功，缺失时返回 None | ⬜ |
| T-S2-B03 | 实现 Agent 三级降级编排 | `agent.py` 实现 `ask()` 方法：CoPaw → 百炼 → Demo 顺序尝试，**不可跳级**，降级静默执行，每次降级记录日志（含降级原因和切换时间）；Demo 模式纯字符串拼接，始终可用 | `07` §1.2, `08` §4 | 三级降级链路正确，日志记录完整 | ⬜ |
| T-S2-B04 | 实现 Storage 层 — QARecord | `storage.py` 实现 `add_record()`、`get_records_by_session()`、`delete_records_by_session()`；写入记录时更新 session.query_count 和 updated_at | `10` §4, §7.2 | 记录 CRUD 通过，query_count 自增正确 | ⬜ |
| T-S2-B05 | 实现首次问答自动命名 | `query_count` 从 0→1 时，session.title 自动设为 query 前 20 字 + "..."；后续不再自动更名 | `10` §6 | 首次提问后标题自动更新，二次提问标题不变 | ⬜ |
| T-S2-B06 | 实现 POST /ask 端点 | 校验 query 非空（→ `EMPTY_QUERY`）、≤500 字符（→ `INVALID_QUERY`）、session_id 有效（→ `INVALID_SESSION_ID`）；调用 Agent.ask()，返回 answer/llm_used/model/response_time_ms/answer_source/traceId | `09` §3, §11 | TC-M01-001～003 通过 | ⬜ |
| T-S2-B07 | 实现 GET /sessions/\<id\>/records 端点 | 校验 session_id 格式和存在性，返回 records 数组，每条含 record_id/query/answer/answer_source/llm_used/model/response_time_ms/timestamp/sources | `09` §7 | TC-M01-030～033 通过 | ⬜ |
| T-S2-B08 | 问答核心集成测试 | 覆盖 TC-M01-001～004（问答成功、空查询、超长查询、降级）、TC-M01-030～031（记录查询、空记录） | `13` §3.2 | 全部测试用例通过 | ⬜ |

### 前端任务

| Task-ID | 任务名称 | 详细描述 | 关联 Spec | DoD（完成标准） | 状态 |
|---------|----------|----------|-----------|----------------|------|
| T-S2-F01 | 实现常见问题网格（B 状态） | 新建会话后自动进入此页面：显示常见问题网格（行业趋势、股票推荐、研报解读），点击自动填入输入框；底部显示输入区域 | `06` §4 状态 B | 新建会话后直接看到常见问题，点击后填入输入框 | ⬜ |
| T-S2-F02 | 实现输入区域 | textarea（3 行，placeholder "请输入您的问题..."）、发送按钮（Ctrl+Enter 快捷键）、清空按钮；发送时 disabled + "发送中…"；无会话时 disabled | `06` §5 | 输入区交互符合规格 | ⬜ |
| T-S2-F03 | 实现对话历史列表（C 状态） | 选中会话后调用 `GET /sessions/<id>/records`，渲染对话卡片：用户提问（右气泡）+ AI 回答（左气泡）+ 来源标签（颜色按 answer_source 区分）+ 响应时间 + 时间戳 | `06` §4 状态 C, §4.1, §4.2 | 对话卡片渲染正确，标签颜色一致 | ⬜ |
| T-S2-F04 | 实现问答提交流程 | 用户输入 → 调用 `POST /ask` → 等待响应 → 追加到对话列表 → 滚动到底部；加载中显示打字动画 | `06` §4.2, §5 | 提问→回答完整流程可走通 | ⬜ |
| T-S2-F05 | 实现前端错误处理 | 按 `06` §6 映射表处理错误码：EMPTY_QUERY（输入框提示）、INVALID_QUERY（字数提示）、SESSION_NOT_FOUND（Toast + 刷新列表）、LLM_UNAVAILABLE（降级提示） | `06` §6 | 各错误场景提示文案正确 | ⬜ |
| T-S2-F06 | API 客户端扩展 | `api.js` 增加 `submitQuestion()`、`getRecords()` 方法 | `08` §3 | API 调用正常 | ⬜ |

---

## S3 — 研报上传与对比（REQ-002 全部 + REQ-003 / US-002 + US-003）

> **交付目标**：文件上传解析 + 研报对比查询页，可演示 PDF/Word/Excel 上传→AI 提取重点 + 多维度筛选分页浏览
> **验证**：TC-M01-048～051 全绿
> **前置条件**：S2 问答核心完成

### 后端任务

| Task-ID | 任务名称 | 详细描述 | 关联 Spec | DoD（完成标准） | 状态 |
|---------|----------|----------|-----------|----------------|------|
| T-S3-B01 | 实现文件上传处理器 | `upload_handler.py` 实现文件接收、格式校验（仅 pdf/docx/xlsx）、大小校验（≤10MB）；使用 pdfplumber/python-docx/openpyxl 解析文件提取 key_points（rating/target_price/core_viewpoints）| `08` §4, `09` §8 | PDF/Word/Excel 文件解析成功返回 key_points | ⬜ |
| T-S3-B02 | 实现 Storage 层 — UploadFile | `storage.py` 实现 `save_upload()`、`get_upload_by_id()`；存储 file_id/session_id/filename/file_size/file_type/upload_status/processing_status/key_points/uploaded_at | `10` §5, §7.3 | 上传文件记录 CRUD 正确 | ⬜ |
| T-S3-B03 | 实现 POST /upload 端点 | `multipart/form-data` 接收文件 + session_id，格式和大小校验失败返回对应错误码（`FILE_TOO_LARGE` / `UNSUPPORTED_FORMAT`），session 不存在返回 404；成功返回 file_id/filename/file_type/processing_status/key_points | `09` §8 | TC-M01-048, 049, 048a, 048b 通过 | ⬜ |
| T-S3-B04 | 实现 Storage 层 — Report | `storage.py` 实现 `query_reports()`（多维度模糊匹配 + 分页）、`add_report()`；Report 字段含 report_id/title/company/industry/publisher/publish_date/rating/target_price/score/tags | `10` §6, §7.4 | 研报查询分页正确，筛选条件生效 | ⬜ |
| T-S3-B05 | 实现 GET /research/comparison 端点 | 查询参数：company/industry/publisher/start_date/end_date（模糊匹配），page（≥1）/page_size（1-100，默认 10）；参数校验失败返回 `INVALID_PAGE` / `INVALID_PAGE_SIZE`；返回 total/page/page_size/reports | `09` §9 | TC-M01-050, 050a, 051, 051a 通过 | ⬜ |
| T-S3-B06 | 准备种子数据 | 在 `data/` 下创建 `reports.json`，填入 ≥10 条研报种子数据（覆盖不同公司、行业、评级、评分） | `04` §6 | 种子数据 ≥10 条，字段完整 | ⬜ |
| T-S3-B07 | 研报功能集成测试 | 覆盖 TC-M01-048～051 全部用例：正常上传、超限、格式错误、正常查询、分页参数错误 | `13` §3.3, §3.4 | 全部测试用例通过 | ⬜ |

### 前端任务

| Task-ID | 任务名称 | 详细描述 | 关联 Spec | DoD（完成标准） | 状态 |
|---------|----------|----------|-----------|----------------|------|
| T-S3-F01 | 实现文件上传功能 | 上传按钮触发文件选择（仅 pdf/docx/xlsx），调用 `POST /upload`，显示上传进度（uploadProgress 状态），超 10MB 前端拦截提示 | `06` §5, `05` AC-002-01/02 | 文件上传成功并显示 key_points | ⬜ |
| T-S3-F02 | 实现暂停生成功能 | 大文件处理中显示暂停按钮，点击后中止请求 | `06` §5, `05` AC-002-03 | 暂停按钮可中止上传/处理 | ⬜ |
| T-S3-F03 | 实现研报对比页面（D 状态） | 新增路由 `/comparison`，实现研报列表页：表格展示 report 字段 + 评分标签 + 分页控件（每页 10 条） | `06` §4 状态 D, `05` AC-003-01 | 研报列表正确渲染，分页可用 | ⬜ |
| T-S3-F04 | 实现研报筛选功能 | 提供公司、行业、发布人、时间四个筛选维度，调用 `GET /research/comparison` 带查询参数 | `06` §4 状态 D, `05` AC-003-02 | 筛选条件变更后列表正确刷新 | ⬜ |
| T-S3-F05 | API 客户端扩展 | `api.js` 增加 `uploadFile()`、`getResearchComparison()` 方法 | `08` §3 | API 调用正常 | ⬜ |

---

## S4 — 收口与发布（REQ-004 + 权限控制 / US-004）

> **交付目标**：健康检查 + 权限控制 + E2E 全链路验证，可演示能力状态芯片 + 无权限 403 + 3 分钟全流程
> **验证**：TC-M01-052～055, E2E 全绿
> **前置条件**：S3 研报功能完成

### 后端任务

| Task-ID | 任务名称 | 详细描述 | 关联 Spec | DoD（完成标准） | 状态 |
|---------|----------|----------|-----------|----------------|------|
| T-S4-B01 | 实现 GET /health 端点 | 检查三个组件（llm_service/file_storage/database）状态，聚合为整体 status（healthy/degraded/unhealthy）；返回 status/timestamp/components/uptime_seconds | `09` §10, `05` AC-004-01/02/03 | TC-M01-052, 053, 055 通过 | ⬜ |
| T-S4-B02 | 实现 GET /capabilities 端点 | 探测当前可用 LLM 提供商，返回各提供商状态（copaw/bailian/demo 的可用性） | `09` §1 | TC-M01-054 通过 | ⬜ |
| T-S4-B03 | 实现 RBAC 权限控制中间件 | 每次 API 请求前验证 `user.role`，无权限返回 403 `PERMISSION_DENIED`；角色矩阵：投研分析师（全部操作）、高级分析师（全部操作）、投资经理（除上传外）、系统管理员（全部） | `11` §2.1, §2.2 | 无权限用户访问时返回 403 | ⬜ |
| T-S4-B04 | 实现速率限制 | 100 请求/分钟/用户，超出返回 429；防止接口滥用 | `07` §4, `11` §4 | 超频请求被正确拦截 | ⬜ |
| T-S4-B05 | 实现审计日志 | 权限验证失败记录日志（用户ID/时间/IP/端点），保留 180 天；文件上传记录日志（用户ID/文件名/大小/结果），保留 365 天；降级事件记录日志（原因/时间/provider），保留 365 天 | `11` §6 | 审计日志按规格记录 | ⬜ |
| T-S4-B06 | 健康检查与权限集成测试 | 覆盖 TC-M01-052～055：健康检查响应结构、组件状态、能力探测、Schema 契约 | `13` §3.5 | 全部测试用例通过 | ⬜ |
| T-S4-B07 | 输入校验完整性检查 | 对照 `09` §11 参数校验规则汇总，逐条验证所有端点的输入校验覆盖完整 | `09` §11 | 11 条校验规则全部有测试覆盖 | ⬜ |

### 前端任务

| Task-ID | 任务名称 | 详细描述 | 关联 Spec | DoD（完成标准） | 状态 |
|---------|----------|----------|-----------|----------------|------|
| T-S4-F01 | 实现能力状态芯片 | Header 区域调用 `GET /capabilities`，按 answer_source 显示对应芯片：CoPaw 已连接 / 百炼·{model} / 离线演示模式 | `06` §2.1 | 芯片颜色和文案与规格一致 | ⬜ |
| T-S4-F02 | 实现权限控制前端 | 显示当前用户角色（user.role）；无权限时全屏提示"您没有访问权限，请联系管理员"，禁用所有操作按钮 | `06` §2.2, §6 | 无权限用户看到 403 提示，按钮 disabled | ⬜ |
| T-S4-F03 | 前端错误处理完善 | 补全所有错误码的前端处理：FILE_TOO_LARGE（Toast）、UNSUPPORTED_FORMAT（Toast）、PERMISSION_DENIED（全屏提示）、LLM_UNAVAILABLE（降级提示） | `06` §6 | 全部错误码有对应 UI 反馈 | ⬜ |

### 验收与质量任务

| Task-ID | 任务名称 | 详细描述 | 关联 Spec | DoD（完成标准） | 状态 |
|---------|----------|----------|-----------|----------------|------|
| T-S4-Q01 | E2E 全链路测试 | 使用 Playwright/Cypress 编写 E2E 测试：新建会话 → 提问 → 查看历史 → 上传研报 → 研报对比 → 删除会话，3 分钟内完成 | `13` §L4, `04` §6 | E2E 测试通过，演示流程 ≤3 分钟 | ⬜ |
| T-S4-Q02 | 代码质量门禁配置 | 配置 `black --check` + `flake8` lint 检查；pytest 单元+集成测试全绿；P0 验收标准全覆盖 | `13` §四 | G-LINT / G-UNIT / G-INT / G-AC 全部通过 | ⬜ |
| T-S4-Q03 | 性能验证 | 验证各端点响应时间：问答 P95 < 5000ms、文件上传 < 10000ms、研报对比 < 2000ms、会话管理 < 500ms | `07` §1.1 | 性能 SLO 达标（或标记 warning） | ⬜ |
| T-S4-Q04 | 需求追踪矩阵更新 | 开发完成后更新 `14` 需求追踪矩阵，确保 API 覆盖率、REQ 覆盖率、TC 覆盖率均达到 100% | `14` §6 | 覆盖率从 22%/45%/25% 提升到 100% | ⬜ |

---

## 任务依赖关系

```
S1 会话管理
  T-S1-B01 → T-S1-B02 → T-S1-B03/B04/B05（并行）→ T-S1-B06 → T-S1-B07
  T-S1-F01 → T-S1-F02 → T-S1-F07 → T-S1-F03/F04/F05/F06（并行）

S2 问答核心（依赖 S1 完成）
  T-S2-B01/B02（并行）→ T-S2-B03 → T-S2-B04 → T-S2-B05 → T-S2-B06/B07（并行）→ T-S2-B08
  T-S2-F06 → T-S2-F01/F02/F03（并行）→ T-S2-F04 → T-S2-F05

S3 研报上传与对比（依赖 S2 完成）
  T-S3-B01/B02（并行）→ T-S3-B03 → T-S3-B04 → T-S3-B05 → T-S3-B06 → T-S3-B07
  T-S3-F05 → T-S3-F01/F02（并行）→ T-S3-F03 → T-S3-F04

S4 收口与发布（依赖 S3 完成）
  T-S4-B01/B02/B03（并行）→ T-S4-B04 → T-S4-B05 → T-S4-B06/B07（并行）
  T-S4-F01/F02（并行）→ T-S4-F03
  T-S4-Q01 → T-S4-Q02 → T-S4-Q03 → T-S4-Q04
```

## 任务统计

| 阶段 | 后端任务 | 前端任务 | 质量任务 | 合计 |
|------|----------|----------|----------|------|
| S1 会话管理 | 7 | 7 | — | 14 |
| S2 问答核心 | 8 | 6 | — | 14 |
| S3 研报上传与对比 | 7 | 5 | — | 12 |
| S4 收口与发布 | 7 | 3 | 4 | 14 |
| **合计** | **29** | **21** | **4** | **54** |

## REQ → Task 追溯

| 需求编号 | 用户故事 | 关联 Task | 验证 TC |
|----------|----------|-----------|---------|
| REQ-001 | US-001 会话管理 | T-S1-B02～B07, T-S1-F03～F06 | TC-M01-020～025 |
| REQ-002 | US-002 问答提交 | T-S2-B01～B08, T-S2-F01～F05, T-S3-B01～B03, T-S3-F01～F02 | TC-M01-001～004, 048～049 |
| REQ-003 | US-003 历史与对比 | T-S2-B04/B07, T-S3-B04～B06, T-S3-F03～F04 | TC-M01-030～031, 050～051 |
| REQ-004 | US-004 健康检查 | T-S4-B01～B02, T-S4-F01 | TC-M01-052～055 |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-04-14 | 基于 Spec 文档自动生成 |
