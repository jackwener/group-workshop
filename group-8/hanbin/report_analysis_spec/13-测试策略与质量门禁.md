# 13 — 测试策略与质量门禁

---

| 项 | 值 |
|---|---|
| 模块编号 | M8-RA |
| 模块名称 | 研报分析助手 |
| 文档版本 | v0.1 |
| 阶段 | Test（断言 — 怎样算通过） |
| 追溯文档 | `05` UserStory · `09` API 接口规格 · `14` 追踪矩阵 |

---

## 一、测试分层

> 共 4 层，写测试前先搞清楚每层验什么。

| 层级 | 工具 | 覆盖对象 | 说明 |
|------|------|----------|------|
| **L1 Unit** | pytest | 单个函数/类的逻辑 | 不依赖网络，最快 |
| **L2 Integration** | pytest + Flask test_client | API 端点 + 存储 | 模拟 HTTP 请求 |
| **L3 Contract** | pytest + schema 断言 | 响应体结构与 `09` 一致 | 验字段名、类型 |
| **L4 E2E** | Playwright / Cypress | 完整用户旅程 | 浏览器级操作 |

## 二、用例拆分原则

> **核心理念：数"可独立失败的点"** — 每条 TC 只回答一个问题，失败时不看代码就知道哪里坏了。

| 来源 | 计数方法 | 示例 |
|------|----------|------|
| 正常路径 | 每个端点的 happy path = 1 条 | `POST /upload → 201` = TC-001 |
| 错误码 | 每个 distinct error code = 1 条 | `INVALID_FILE_TYPE` = TC-002 |
| 分支路径 | 每个 if/else 降级分支 = 1 条 | 无 API Key → demo = TC-004 |
| 类型约束 | Spec `09` 约定的字段类型 = 1 条 | `llm_used` 必须 bool = TC-006 |

## 三、测试用例清单

### 3.1 研报上传与解析 API 测试

> 覆盖 POST /upload。文件上传、解析、错误处理。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M8RA-001 | Integration | `POST /upload` → 201, 返回 report_id、提取的字段完整 | REQ-001 | P0 |
| TC-M8RA-002 | Integration | PDF格式文件上传成功，解析返回正确 | REQ-001 | P0 |
| TC-M8RA-003 | Integration | HTML格式文件上传成功，解析返回正确 | REQ-001 | P0 |
| TC-M8RA-004 | Integration | 非PDF/HTML/Word/PPT文件 → 400, `INVALID_FILE_TYPE` | REQ-001 | P0 |
| TC-M8RA-005 | Integration | 文件>50MB → 400, `FILE_TOO_LARGE` | REQ-001 | P0 |

### 3.2 研报问答 API 测试

> 覆盖 POST /ask。问答交互、降级策略。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M8RA-006 | Integration | `POST /ask` → 200, 含 answer、llm_used、traceId | REQ-002 | P0 |
| TC-M8RA-007 | Integration | 空 query → 400, `EMPTY_QUERY` | REQ-002 | P0 |
| TC-M8RA-008 | Integration | query >500字符 → 400, `INVALID_QUERY` | REQ-002 | P0 |
| TC-M8RA-009 | Integration | 无 LLM 配置 → `answer_source='demo'` | REQ-002 | P0 |
| TC-M8RA-010 | Integration | 有百炼配置 → `answer_source='bailian'` | REQ-002 | P1 |

### 3.3 会话管理 API 测试

> 覆盖 GET /sessions、POST /sessions、DELETE /sessions/<id>。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M8RA-011 | Integration | `GET /sessions` → 200, 返回 sessions 数组 | REQ-003 | P0 |
| TC-M8RA-012 | Integration | `POST /sessions` → 201, 含 session_id | REQ-003 | P0 |
| TC-M8RA-013 | Integration | `DELETE /sessions/<id>` → 200, 级联删除关联数据 | REQ-003 | P0 |
| TC-M8RA-014 | Integration | 删除不存在会话 → 404 | REQ-003 | P1 |

### 3.4 研报对比 API 测试

> 覆盖 POST /compare。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M8RA-015 | Integration | `POST /compare` 2份研报 → 200, 对比结果完整 | REQ-004 | P1 |
| TC-M8RA-016 | Integration | `POST /compare` 3份研报 → 200, 响应<10s | REQ-004 | P1 |
| TC-M8RA-017 | Integration | `POST /compare` 1份研报 → 400, 数量不足 | REQ-004 | P1 |

### 3.5 Storage 层单元测试

> 覆盖 Storage 类核心方法。

| TC-ID | 层级 | 覆盖方法 | 断言要点 | 优先级 |
|-------|------|----------|----------|--------|
| TC-M8RA-018 | Unit | `Storage.__init__` | 目录不存在时自动创建 | P0 |
| TC-M8RA-019 | Unit | `create_session` | 创建成功，返回正确结构 | P0 |
| TC-M8RA-020 | Unit | `delete_session` | 删除成功，级联删除关联记录 | P0 |
| TC-M8RA-021 | Unit | `create_report` | 研报记录创建成功 | P0 |
| TC-M8RA-022 | Unit | `add_record` | 问答记录添加，query_count更新 | P0 |

### 3.6 能力探测与问答记录测试

> 覆盖 GET /capabilities 和 GET /sessions/<id>/records。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M8RA-025 | Integration | `GET /capabilities` → 200, 含 bailian_configured、supported_formats | REQ-005 | P1 |
| TC-M8RA-026 | Integration | `GET /sessions/<id>/records` → 200, 返回 records 数组 | REQ-003 | P1 |

### 3.7 外部LLM与安全测试

> 覆盖外部LLM配置、安全需求。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M8RA-023 | Integration | 外部LLM不可用时自动降级到Demo | REQ-005 | P1 |
| TC-M8RA-024 | Unit | API Key 从环境变量读取，非硬编码 | REQ-005 | P1 |

## 四、质量门禁

| Gate-ID | 条件 | 阻塞合并 |
|---------|------|----------|
| **G-LINT** | `black --check` + `flake8` 通过 | **是** |
| **G-UNIT** | Unit 测试全绿 | **是** |
| **G-INT** | Integration 测试全绿 | **是** |
| **G-AC** | P0 验收标准全覆盖 | **是** |
| **G-PERF** | `response_time_ms < 5000` | 否（warning） |

### 执行命令

```bash
# 后端测试
cd group-8/hanbin/report_analysis/backend
python -m pytest tests/ -q

# 后端测试 + 覆盖率
python -m pytest tests/ -q --cov=. --cov-report=term-missing
```

## 五、TDD 适用场景

| 场景 | TDD 价值 | 建议 |
|------|----------|------|
| 简单 CRUD（列表、删除） | 低 | 写完补测试 |
| 参数校验 / 错误码 | 中 | 建议 TDD |
| 多级降级 / 状态机 | **高** | **强烈建议 TDD** |
| 研报解析 / 信息提取 | **高** | **建议 TDD** |

## 六、追溯关系

> 汇总所有 TC 与 REQ 的映射，完成后同步到 `14` 追踪矩阵。

| 需求编号 | 已实现 TC | 待补充 TC |
|----------|-----------|-----------|
| REQ-001 | TC-M8RA-001~005 | — |
| REQ-002 | TC-M8RA-006~010 | — |
| REQ-003 | TC-M8RA-011~014, TC-M8RA-026 | — |
| REQ-004 | TC-M8RA-015~017 | — |
| REQ-005 | TC-M8RA-023~025 | — |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-04-14 | 首版填写 |
