# S4 — 收口与质量门禁

> **目标**：全链路验收 + 代码质量 + 性能 SLO 检查，确保可演示、可提交。
> **依赖**：S3 完成
> **满足**：全部 REQ 验收 · `13` 质量门禁 · `07` SLO

---

## T-040 P0 Integration 测试补全

**对齐**：`13` §3.1 会话管理 API 测试 · §3.2 问答记录 API 测试

### 文件：`backend/tests/`

### 步骤

- [ ] 1. 补全会话管理 API 测试：

| TC-ID | 断言要点 |
|-------|----------|
| TC-M01-020 | `GET /sessions` → 200，返回 sessions 数组 |
| TC-M01-021 | `POST /sessions` → 201，含 session_id, title, created_at, query_count |
| TC-M01-022 | `POST /sessions` title > 100 字符 → 400 `INVALID_SESSION_TITLE` |
| TC-M01-023 | `DELETE /sessions/<id>` → 200，含 message, deleted_records |
| TC-M01-024 | `DELETE /sessions/<不存在id>` → 404 `SESSION_NOT_FOUND` |
| TC-M01-025 | `DELETE /sessions/<id>` 级联删除记录数正确 |

- [ ] 2. 补全问答 API 测试（对齐已有 TC-M01-001～004）：

| TC-ID | 断言要点 |
|-------|----------|
| TC-M01-005 | `GET /capabilities` → 200，含 copaw_configured, bailian_configured |
| TC-M01-006 | `POST /ask` 返回 `llm_used` 类型为 bool（非字符串） |
| TC-M01-007 | `POST /ask` 缺少 session_id → 400 `MISSING_SESSION_ID` |

### DoD

- [ ] `python -m pytest tests/ -v` 全绿
- [ ] P0 测试用例 ≥ 15 条

---

## T-041 Contract 测试 — 响应体结构

**对齐**：`13` §1 L3 Contract 层

### 文件：`backend/tests/test_contract.py`

### 步骤

- [ ] 1. 对每个端点验证响应体字段结构与 `09` 一致：
  - POST /ask：`traceId(str)`, `answer(str)`, `llm_used(bool)`, `model(str|None)`, `response_time_ms(int)`, `answer_source(str)`
  - POST /sessions：`traceId(str)`, `session_id(str)`, `title(str)`, `created_at(str)`, `query_count(int)`
  - GET /sessions：`traceId(str)`, `sessions(list)`
  - DELETE /sessions：`traceId(str)`, `message(str)`, `deleted_records(int)`
  - GET /records：`traceId(str)`, `session_id(str)`, `records(list)`

- [ ] 2. 验证 `answer_source` 枚举值：只能是 `copaw | bailian | demo`
- [ ] 3. 验证 `traceId` 格式：`tr_` + 32 位 hex

### DoD

- [ ] 所有端点的响应体字段名、类型与 `09` 完全一致
- [ ] 枚举值范围正确

---

## T-042 质量门禁检查

**对齐**：`13` §4 质量门禁

### 步骤

- [ ] 1. **G-LINT**：代码格式检查
  ```bash
  # 后端
  pip install black flake8
  black --check backend/
  flake8 backend/ --max-line-length=120
  
  # 前端
  cd frontend && npx eslint src/
  ```

- [ ] 2. **G-UNIT**：单元测试全绿
  ```bash
  cd backend && python -m pytest tests/ -q
  ```

- [ ] 3. **G-INT**：Integration 测试全绿
  ```bash
  cd backend && python -m pytest tests/ -q --cov=. --cov-report=term-missing
  ```

- [ ] 4. **G-AC**：P0 验收标准全覆盖
  - AC-001-01 会话管理 ✓
  - AC-001-02 研报筛选 ✓
  - AC-001-03 研报详情 ✓
  - AC-002-01 搜索条件返回 ✓
  - AC-003-01 新闻获取 ✓
  - AC-003-02 关键词推送 ✓

- [ ] 5. **G-PERF**：SLO 检查（对齐 `07` §1.1）
  - `response_time_ms < 5000`（问答）
  - 会话管理 < 500ms
  - 研报查询 < 1000ms
  - 新闻推送 < 1000ms

### DoD

- [ ] 全部 Gate 通过（G-LINT, G-UNIT, G-INT, G-AC）
- [ ] G-PERF warning 可接受

---

## T-043 前端 — 页面布局与样式收口

**对齐**：`06` §1 页面总体布局

### 步骤

- [ ] 1. 确认布局结构符合 `06` §1：
  ```
  Header → Sidebar + Main + Input-Area
  ```

- [ ] 2. 响应式适配：
  - B 状态卡片网格：PC 3×2，移动端 1 列
  - Sidebar 可收起/展开（`isSidebarOpen` 状态）

- [ ] 3. 样式统一：
  - 来源标签颜色：copaw 绿色 / bailian 蓝色 / demo 灰色
  - Toast 样式一致
  - 骨架屏动画

- [ ] 4. 检查全部 13 个 state 变量是否已使用（对齐 `06` §7）

### DoD

- [ ] 布局与 `06` §1 ASCII 示意一致
- [ ] PC 和移动端均可用
- [ ] 全部 state 变量已正确管理

---

## T-044 全链路演示验收

**对齐**：`04` §6 验收标准

### 步骤

- [ ] 1. **主路径演示**（3 分钟内完成）：
  1. 启动后端：`python -m flask --app app run --port 5000`
  2. 启动前端：`cd frontend && npm run dev`
  3. 新建会话 → 输入问题 → 获得回答 → 查看来源标签
  4. 查看历史记录 → 切换会话 → 删除会话

- [ ] 2. **离线可用**：未配置密钥时返回 `answer_source=demo`

- [ ] 3. **研报搜索**：按评级/公司/目标价搜索，返回研报列表及比对信息

- [ ] 4. **新闻推送**：新闻获取、关键词展示、条件查询

- [ ] 5. **能力状态**：Header 芯片正确反映配置状态

### DoD

- [ ] 主路径 3 分钟内可演示完毕
- [ ] 离线模式返回 demo 回答
- [ ] 研报搜索和新闻推送全链路可用
- [ ] 无控制台报错
