# 13 — 测试策略与质量门禁（引导版模板）

---

| 项 | 值 |
|---|---|
| 模块编号 | M1-QA |
| 模块名称 | 投研问答助手 |
| 文档版本 | v0.1 |
| 阶段 | Test（断言 — 怎样算通过） |
| 追溯文档 | `05` UserStory · `09` API 接口规格 · `14` 追踪矩阵 |

---

## 一、测试分层

> 共 4 层，写测试前先搞清楚每层验什么。

| 层级 | 工具 | 覆盖对象 | 说明 |
|------|------|----------|------|
| **L1 Unit** | pytest | 单个函数/类的逻辑 | 不依赖网络，最快 |
| **L2 Integration** | pytest + Flask test_client | API 端点 + 存储 | 模拟 HTTP 请求 |
| **L3 Contract** | pytest + schema 断言 | 响应体结构与 `09` 一致 | 验字段名、类型 |
| **L4 E2E** | Playwright / Cypress | 完整用户旅程 | 浏览器级操作 |

## 二、用例拆分原则

> **核心理念：数"可独立失败的点"** — 每条 TC 只回答一个问题，失败时不看代码就知道哪里坏了。

| 来源 | 计数方法 | 示例 |
|------|----------|------|
| 正常路径 | 每个端点的 happy path = 1 条 | `POST /ask → 200` = TC-001 |
| 错误码 | 每个 distinct error code = 1 条 | `EMPTY_QUERY` = TC-002 |
| 分支路径 | 每个 if/else 降级分支 = 1 条 | 无 API Key → demo = TC-004 |
| 类型约束 | Spec `09` 约定的字段类型 = 1 条 | `llm_used` 必须 bool = TC-006 |

## 三、★ 示例：已有测试用例

| TC-ID | 层级 | 测试函数 | 断言要点 | 关联 REQ |
|-------|------|----------|----------|----------|
| TC-M01-001 | Integration | `test_ask_success` | `POST /ask` → 200, 含 answer、llm_used、traceId | REQ-001 |
| TC-M01-002 | Integration | `test_ask_empty_query` | 空 query → 400, `EMPTY_QUERY` | REQ-005 |
| TC-M01-003 | Integration | `test_ask_long_query` | query >500 → 400, `INVALID_QUERY` | REQ-005 |
| TC-M01-004 | Integration | `test_bailian_fallback` | 无 API Key → `answer_source='demo'` | REQ-003 |

### 3.1 会话管理 API 测试

> 覆盖 GET /sessions、POST /sessions、DELETE /sessions/<id>。对应 05 US-001 的 AC-001-01~03。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-020 | L3 API | POST /sessions → 201, 返回 session_id/title/created_at/query_count | REQ-M1QA-001 | P0 |
| TC-M01-021 | L3 API | GET /sessions → 200, 返回 sessions 数组含分页信息(total/page/page_size) | REQ-M1QA-001 | P0 |
| TC-M01-022 | L3 API | GET /sessions?page=2&page_size=10 → 正确分页返回 | REQ-M1QA-001 | P1 |
| TC-M01-023 | L3 API | DELETE /sessions/<id> → 200, 返回 deleted=true, 级联删除问答记录 | REQ-M1QA-001 | P0 |
| TC-M01-024 | L3 API | DELETE 不存在 session → 404 SESSION_NOT_FOUND | REQ-M1QA-001 | P0 |
| TC-M01-025 | L3 API | POST /sessions title>100字符 → 400 INVALID_TITLE | REQ-M1QA-001 | P1 |
| TC-M01-026 | L4 E2E | 点击新建会话 → 会话列表新增，侧栏实时更新 | REQ-M1QA-001 | P0 |
| TC-M01-027 | L4 E2E | 点击切换会话 → 问答区显示该会话历史 | REQ-M1QA-001 | P0 |
| TC-M01-028 | L4 E2E | 点击删除会话 → 会话从列表移除，确认级联删除 | REQ-M1QA-001 | P0 |

### 3.2 问答记录 API 测试

> 覆盖 GET /sessions/<id>/records 和 POST /ask。对应 05 US-002 的 AC-002-01~03。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-030 | L3 API | POST /ask 正常提问 → 200, 返回 answer/llm_used/model/response_time_ms/answer_source | REQ-M1QA-002 | P0 |
| TC-M01-031 | L3 API | POST /ask query为空 → 400 EMPTY_QUERY | REQ-M1QA-002 | P0 |
| TC-M01-032 | L3 API | POST /ask query>500字符 → 400 INVALID_QUERY | REQ-M1QA-002 | P0 |
| TC-M01-033 | L3 API | POST /ask 无效session_id → 400 INVALID_SESSION_ID | REQ-M1QA-002 | P0 |
| TC-M01-034 | L3 API | POST /ask 不存在session → 404 SESSION_NOT_FOUND | REQ-M1QA-002 | P0 |
| TC-M01-035 | L3 API | LLM不可用时 → 降级到Demo, answer_source=demo, llm_used=false | REQ-M1QA-002 | P0 |
| TC-M01-036 | L3 API | GET /sessions/<id>/records → 200, 返回 records 数组含分页 | REQ-M1QA-002 | P0 |
| TC-M01-037 | L3 API | GET /sessions/<id>/records 无记录 → 200, records为空数组 | REQ-M1QA-002 | P1 |
| TC-M01-038 | L3 API | GET /sessions/<id>/records 无效session → 404 SESSION_NOT_FOUND | REQ-M1QA-002 | P0 |
| TC-M01-039 | L4 E2E | 输入问题点击发送 → 答案展示在对话区，含来源标识 | REQ-M1QA-002 | P0 |
| TC-M01-040 | L4 E2E | 询问研报对比 → 表格形式展示关键指标对比 | REQ-M1QA-002 | P0 |

### 3.3 Storage 层单元测试

> 覆盖 Storage 类核心方法，对应 10 数据模型的方法清单。

| TC-ID | 层级 | 覆盖方法 | 断言要点 | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-050 | L1 Unit | `Storage.__init__` | 目录不存在时自动创建，文件不存在时初始化空数组 | P0 |
| TC-M01-051 | L1 Unit | `create_session` | 创建成功返回session dict，query_count=0 | P0 |
| TC-M01-052 | L1 Unit | `create_session` 重复ID | 重复session_id报错或覆盖策略 | P0 |
| TC-M01-053 | L1 Unit | `get_sessions` | 分页正确，按updated_at倒序 | P0 |
| TC-M01-054 | L1 Unit | `get_session_by_id` | 存在返回dict，不存在返回None | P0 |
| TC-M01-055 | L1 Unit | `update_session` | 更新title同时更新updated_at | P1 |
| TC-M01-056 | L1 Unit | `delete_session` | 删除成功返回True，级联删除问答记录 | P0 |
| TC-M01-057 | L1 Unit | `add_record` | 写入记录，同步更新session.query_count和updated_at | P0 |
| TC-M01-058 | L1 Unit | `get_records_by_session` | 分页正确，按timestamp倒序 | P0 |
| TC-M01-059 | L1 Unit | `delete_records_by_session` | 返回删除数量，记录确实被删 | P0 |
| TC-M01-060 | L1 Unit | `create_report` | status初始为pending | P0 |
| TC-M01-061 | L1 Unit | `update_report_status` | status=analyzed时自动更新analyzed_at | P0 |
| TC-M01-062 | L1 Unit | `get_reports` | 支持status筛选，按uploaded_at倒序 | P0 |
| TC-M01-063 | L1 Unit | `delete_report` | 删除成功返回True | P0 |

### 3.4 研报管理 API 测试

> 覆盖 POST/GET/DELETE /reports 和 POST /reports/<id>/analyze。对应 05 US-003 的 AC-003-01~03。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-070 | L3 API | POST /reports 上传PDF → 201, 返回 report_id/status=pending | REQ-M1QA-003 | P0 |
| TC-M01-071 | L3 API | POST /reports 空文件 → 400 EMPTY_FILE | REQ-M1QA-003 | P0 |
| TC-M01-072 | L3 API | POST /reports 无效类型 → 400 INVALID_FILE_TYPE | REQ-M1QA-003 | P0 |
| TC-M01-073 | L3 API | POST /reports 文件>50MB → 400/413 错误 | REQ-M1QA-003 | P0 |
| TC-M01-074 | L3 API | GET /reports → 200, 返回 reports 数组含分页 | REQ-M1QA-003 | P0 |
| TC-M01-075 | L3 API | GET /reports?status=analyzed → 正确筛选 | REQ-M1QA-003 | P1 |
| TC-M01-076 | L3 API | DELETE /reports/<id> → 200, 文件同步删除 | REQ-M1QA-003 | P0 |
| TC-M01-077 | L3 API | POST /reports/<id>/analyze → 200, 返回 analysis 结构 | REQ-M1QA-003 | P0 |
| TC-M01-078 | L3 API | 分析已缓存研报 → 直接返回缓存，不重复调用LLM | REQ-M1QA-003 | P1 |
| TC-M01-079 | L4 E2E | 拖拽上传研报 → 文件出现在列表，状态变为analyzed | REQ-M1QA-003 | P0 |

### 3.5 其他功能 API 测试

> 覆盖健康检查、历史记录、导出功能。对应 05 US-004~006。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-080 | L3 API | GET /health → 200, 返回 status/services/metrics | REQ-M1QA-004 | P1 |
| TC-M01-081 | L3 API | GET /health LLM不可用时 → status=degraded | REQ-M1QA-004 | P1 |
| TC-M01-082 | L3 API | GET /sessions/<id>/records 支持搜索关键词 | REQ-M1QA-005 | P1 |
| TC-M01-083 | L3 API | POST /export → 200, 返回 export_id/status=processing | REQ-M1QA-006 | P2 |
| TC-M01-084 | L3 API | POST /export report_ids<2 → 400 INVALID_REPORT_COUNT | REQ-M1QA-006 | P2 |

## 四、质量门禁

| Gate-ID | 条件 | 阈值 | 阻塞合并 |
|---------|------|------|----------|
| **G-LINT** | 代码格式检查 | `black --check` 通过 + `flake8` 无error | **是** |
| **G-UNIT** | L1单元测试 | 覆盖率 ≥ 80%，全部通过 | **是** |
| **G-INT** | L2/L3集成/API测试 | 全部通过，无失败用例 | **是** |
| **G-AC** | P0验收标准覆盖 | 05中所有P0的AC均有对应TC且通过 | **是** |
| **G-PERF** | 性能指标 | P95延迟 < 5000ms (07 §1.1) | 否（warning） |
| **G-E2E** | L4端到端测试 | 核心用户旅程全部通过 | **是** |

### 执行命令

```bash
# 后端测试
cd groups/Group-X/NN/backend
python -m pytest tests/ -q

# 后端测试 + 覆盖率
python -m pytest tests/ -q --cov=. --cov-report=term-missing
```

## 五、TDD 适用场景

| 场景 | TDD 价值 | 建议 |
|------|----------|------|
| 简单 CRUD（列表、删除） | 低 | 写完补测试 |
| 参数校验 / 错误码 | 中 | 建议 TDD |
| 多级降级 / 状态机 | **高** | **强烈建议 TDD** |
| 金融计算 / 风险判定 | **极高** | **必须 TDD** |

## 六、追溯关系

> 汇总所有 TC 与 REQ 的映射，完成后同步到 `14` 追踪矩阵。

| 需求编号 | 对应 AC | 已实现 TC | TC覆盖状态 |
|----------|---------|-----------|------------|
| REQ-M1QA-001 | AC-001-01~03 | TC-M01-020~028 | 100%覆盖 |
| REQ-M1QA-002 | AC-002-01~03 | TC-M01-030~040 | 100%覆盖 |
| REQ-M1QA-003 | AC-003-01~03 | TC-M01-070~079 | 100%覆盖 |
| REQ-M1QA-004 | AC-004-01~02 | TC-M01-080~081 | 100%覆盖 |
| REQ-M1QA-005 | AC-005-01~03 | TC-M01-036~037, 082 | 100%覆盖 |
| REQ-M1QA-006 | AC-006-01~03 | TC-M01-083~084 | 100%覆盖 |

### 全链路追溯表

| API端点 | 参数校验规则 | 对应TC | 对应AC | 对应Storage方法 |
|---------|-------------|--------|--------|----------------|
| POST /sessions | title≤100字符 | TC-M01-025 | AC-001-01 | create_session |
| GET /sessions | page/page_size分页 | TC-M01-021 | AC-001-02 | get_sessions |
| DELETE /sessions/<id> | session存在 | TC-M01-023~024 | AC-001-03 | delete_session |
| POST /ask | query非空/≤500字符/session_id有效 | TC-M01-030~035 | AC-002-01~02 | add_record |
| GET /sessions/<id>/records | session存在/分页 | TC-M01-036~038 | AC-005-01 | get_records_by_session |
| POST /reports | 文件非空/类型/大小 | TC-M01-070~073 | AC-003-01 | create_report |
| GET /reports | status筛选/分页 | TC-M01-074~075 | AC-003-02 | get_reports |
| DELETE /reports/<id> | report存在 | TC-M01-076 | AC-003-02 | delete_report |
| POST /reports/<id>/analyze | report存在 | TC-M01-077~078 | AC-003-03 | update_report_status |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2025-04-14 | 首版填写，基于05 AC、09 API、10 Storage方法定义完整测试用例集 |
