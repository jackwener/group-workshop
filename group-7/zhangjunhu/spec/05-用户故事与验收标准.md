# 05 — 用户故事与验收标准（引导版模板）

---

| 项 | 值 |
|---|---|
| 模块编号 | M1-QA |
| 模块名称 | 投研问答助手 |
| 文档版本 | v0.1 |
| 阶段 | Spec（What） |
| 上游 | `03` 立项提案 · `04` PRD |
| 下游 | → `06` FSD · `09` API · `13` 测试 · `14` 追踪 |

---

> **本文只做一件事**：按用户能理解的子场景拆成多条 **User Story**，并为每条写清 **REQ-ID**、**验收标准（AC）**。

## 字段说明 — 每条 UserStory 必须包含以下字段

| 字段 | 填写说明 | 数据来源 ← | 流向下游 → |
|------|----------|------------|-----------|
| **需求编号** | 唯一编号，如 REQ-M1QA-001 | ← `02` 采集编号 | → `14` 追踪矩阵首列 |
| **故事** | 作为【角色】，我希望【行为】，以便【价值】 | ← `04` PRD 场景拆解 | → `06` FSD 功能点 |
| **优先级** | P0（必须）/ P1（应该）/ P2（可选） | ← `03` §4 成功标准 | → `12` 里程碑排期 |
| **来源** | 对应 03/04 哪一条需求 | ← `03` 范围 · `04` 场景 | |
| **前置条件** | 触发该故事的前提状态 | ← `04` 用户流程 | → `13` 测试前置 |
| **功能对齐** | 对应功能规格 + 非功能约束 | | → `06` FSD · `07` NFR |
| **设计对齐** | 对应架构、接口、数据、安全 | | → `08` 架构 · `09` API · `10` Data · `11` 安全 |
| **验证对齐** | 对应测试用例 + 追踪矩阵行 | | → `13` TC · `14` 追踪矩阵行 |
| **验收标准** | AC，每条可独立验证 | ← 故事 + `07` NFR | → `13` 测试用例依据 |

## US-001（P0）会话管理

| 字段 | 内容 |
|------|------|
| **需求编号** | REQ-M1QA-001 |
| **故事** | 作为投研总监，我希望能创建、删除和查询我的会话，以便回溯参考 |
| **优先级** | P0 |
| **来源** | ← 03 §2 / 04 §____ |
| **前置条件** | 用户已登录系统 |
| **功能对齐** | → 06 §____ · 07 §____ |
| **设计对齐** | → 08 §____ · 09 ____API · 10 ____表 · 11 §____ |
| **验证对齐** | → 13 TC-____ · 14 第__行 |

| 验收标准 | 描述（每条可独立测试） |
|----------|----------------------|
| AC-001-01 | 点击新建会话，自动打开新会话TAB页，页面title位置可显示基于问答提炼的核心关键词|
| AC-001-02 | 用户删除历史会话，不可恢复；当前会话不可删除 |
| AC-001-03 | 可查询最近30天会话历史，按时间倒序 |

## US-002（P0）问答提交

| 字段 | 内容 |
|------|------|
| **需求编号** | REQ-M1QA-002 |
| **故事** | 作为分析师，我希望输入问题或关键词后，能获取到连续准确的，以便快研报关键信息输出；并且可以指定输出的研报内容的范围 |
| **优先级** | P0 |
| **来源** | ← 03 §2 / 04 §____ |
| **前置条件** | 已创建会话 |
| **功能对齐** | → 06 §____ · 07 §____ |
| **设计对齐** | → 08 §____ · 09 ____API · 10 ____表 · 11 §____ |
| **验证对齐** | → 13 TC-____ · 14 第__行 |

| 验收标准 | 描述（每条可独立测试） |
|----------|----------------------|
| AC-002-01 | 用户提问某家券商的研报，对话窗口流式回复提炼后的研报关键信息 |
| AC-002-02 | 用户提问某些指标的研报信息，对话窗口流式回复各券商相关研报关键指标的横向对比清单 |
| AC-002-03 | 用户提问某些券商某些范围的内容，对话窗口流式回复相关内容，按券商列举清单 |

## US-003（P0）历史记录

| 字段 | 内容 |
|------|------|
| **需求编号** | REQ-M1QA-003 |
| **故事** | 作为分析师，我希望查看问答历史记录，以便回顾之前的分析内容和结论。同时在新的提问中能自动关联到我的历史记录。历史记录支持基于关键词的查找定位 |
| **优先级** | P0 |
| **来源** | ← 03 §2 / 04 §____ |
| **前置条件** | 已有问答记录 |
| **功能对齐** | → 06 §____ · 07 §____ |
| **设计对齐** | → 08 §____ · 09 ____API · 10 ____表 · 11 §____ |
| **验证对齐** | → 13 TC-____ · 14 第__行 |

| 验收标准 | 描述（每条可独立测试） |
|----------|----------------------|
| AC-003-01 | 用户可查看历史问答记录，可查看根据关键词定位历史记录 |
| AC-003-02 | 用户在新的会话中出现历史相似性提问时，优先展示历史记录，并给出是否进行新的回复的确认 |

## US-004（P1）股票信息查询

| 字段 | 内容 |
|------|------|
| **需求编号** | REQ-M1QA-004 |
| **故事** | 作为投研经理，我希望在查看研报时，对其中涉及的股票产品，可便捷查看股票的信息 |
| **优先级** | P1 |
| **来源** | ← 03 §5 / 04 §____ |
| **前置条件** | 系统已部署 |
| **功能对齐** | → 06 §____ · 07 §____ |
| **设计对齐** | → 08 §____ · 09 ____API · 10 ____表 · 11 §____ |
| **验证对齐** | → 13 TC-____ · 14 第__行 |

| 验收标准 | 描述（每条可独立测试） |
|----------|----------------------|
| AC-004-01 | 研报中的股票产品显式标记，支持触发式查询，用户点击查询时以新的会话窗口回复 |
| AC-004-02 | 用户在不同的研报问答窗口中触发同一只股票的信息查询时，近打开一个股票回复窗口 |

## 需求追溯总索引 — 每条需求对应哪些下游文档

| 需求编号 | UserStory | 优先级 | →06 功能 | →09 接口 | →11 安全 | →13 测试 | →14 追溯 |
|----------|-----------|--------|----------|----------|----------|----------|----------|
| REQ-001 | 会话管理 | **P0** | §____ | ____API | §____ | TC-____ | 行____ |
| REQ-002 | 问答提交 | **P0** | §____ | ____API | §____ | TC-____ | 行____ |
| REQ-003 | 历史记录 | **P0** | §____ | ____API | §____ | TC-____ | 行____ |
| REQ-004 | 股票信息查询 | **P1** | §____ | ____API | §____ | TC-____ | 行____ |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-04-14 | 首版填写 |
