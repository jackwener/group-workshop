# 13 — 测试策略与质量门禁

---

| 项 | 值 |
|---|---|
| 模块编号 | M4-QA |
| 模块名称 | 研报问答助手 |
| 文档版本 | v0.1 |
| 阶段 | Test（OK?） |
| 上游 | `05` UserStory · `09` API |
| 下游 | → `14` 追踪 |

---

## 1. 测试策略概览

```
┌─────────────────────────────────────────────────────┐
│                    测试金字塔                        │
├─────────────────────────────────────────────────────┤
│                    E2E Tests (10%)                  │
│              ┌─────────────────┐                    │
│              │  Integration    │                    │
│              │     Tests       │                    │
│              │     (30%)       │                    │
│              └─────────────────┘                    │
│         ┌───────────────────────────┐               │
│         │      Unit Tests (60%)     │               │
│         └───────────────────────────┘               │
└─────────────────────────────────────────────────────┘
```

## 2. 测试用例清单

### TC-001 会话管理测试

| 用例ID | 测试场景 | 前置条件 | 测试步骤 | 预期结果 |
|--------|----------|----------|----------|----------|
| TC-001-01 | 创建会话 | 无 | POST /api/sessions | 返回session_id |
| TC-001-02 | 获取会话列表 | 已创建会话 | GET /api/sessions | 返回会话列表 |
| TC-001-03 | 删除会话 | 已创建会话 | DELETE /api/sessions/{id} | 返回success |
| TC-001-04 | 删除不存在的会话 | 无 | DELETE /api/sessions/invalid | 返回404 |

### TC-002 问答测试

| 用例ID | 测试场景 | 前置条件 | 测试步骤 | 预期结果 |
|--------|----------|----------|----------|----------|
| TC-002-01 | 正常问答 | 已创建会话 | POST /api/qa/ask | 返回answer |
| TC-002-02 | 空问题 | 已创建会话 | POST空query | 返回验证错误 |
| TC-002-03 | 超长问题 | 已创建会话 | POST 501字符query | 返回验证错误 |
| TC-002-04 | 无效会话ID | 无 | POST无效session_id | 返回404 |
| TC-002-05 | 获取历史 | 已有问答 | GET /api/qa/history | 返回历史记录 |

### TC-003 历史记录测试

| 用例ID | 测试场景 | 前置条件 | 测试步骤 | 预期结果 |
|--------|----------|----------|----------|----------|
| TC-003-01 | 查询历史记录 | 已有问答 | GET历史记录 | 返回记录列表 |
| TC-003-02 | 分页查询 | 已有多条记录 | GET带分页参数 | 返回分页数据 |

### TC-004 健康检查测试

| 用例ID | 测试场景 | 前置条件 | 测试步骤 | 预期结果 |
|--------|----------|----------|----------|----------|
| TC-004-01 | 正常健康检查 | 服务正常 | GET /api/health | 返回healthy |
| TC-004-02 | 降级健康检查 | 部分服务不可用 | GET /api/health | 返回degraded |

## 3. 质量门禁

### 代码质量门禁

| 指标 | 阈值 | 工具 |
|------|------|------|
| 代码覆盖率 | ≥ 80% | pytest-cov |
| 代码规范 | 无严重问题 | ruff, eslint |
| 安全扫描 | 无高危漏洞 | bandit, npm audit |

### 功能质量门禁

| 指标 | 阈值 |
|------|------|
| P0用例通过率 | 100% |
| P1用例通过率 | ≥ 95% |
| 阻塞缺陷 | 0 |

### 性能质量门禁

| 指标 | 阈值 |
|------|------|
| API响应时间 P95 | < 3s |
| 并发用户数 | ≥ 100 |

## 4. 测试环境

| 环境 | 用途 | 配置 |
|------|------|------|
| Dev | 开发自测 | 本地Docker |
| Test | 集成测试 | 独立服务器 |
| Staging | 预发布 | 类生产环境 |
| Prod | 生产环境 | 生产集群 |

## 5. 测试数据

| 数据类型 | 数量 | 说明 |
|----------|------|------|
| 种子会话 | 10 | 测试会话管理 |
| 种子问答 | 100 | 测试问答功能 |
| 种子研报 | 50 | 测试向量检索 |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-04-14 | 首版填写 |
