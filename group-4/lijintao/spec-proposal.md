以下是对该立项提案与范围说明文档的优化版本，在保留原有结构的基础上，提升了内容的规范性、可读性与完整性，同时补充了合理的示例内容（若为模板则提供填写指引，若为实际项目则填充逻辑内容）：

Proposal - 立项提案与范围说明

|模块编号|【填写：如 M1-QA】|模块名称|【填写：如 投研问答助手】|
| --- | --- | --- | --- |
|文档版本|v0.1|阶段|Proposal（Why）|
|技术栈|后端：Flask + JSON 文件存储；前端：React + Vite + pytest 测试|追溯|→ 本文通过后进入 PRD 阶段|

1. 背景与问题

目标：描述用户遇到的核心问题（至少 2 - 3 条痛点），参考维度：信息获取效率、历史记录管理、可追溯性、系统可用性等。

|痛点描述|来源|
| --- | --- |
|信息分散：研报、新闻、数据分布在多个系统，无统一问答入口|用户访谈|
|信息阅读效率低：每天花费大量时间查找/整理关键信息，无法快速验证信息有效性|邮件沟通（或用户调研问卷）|

1. 提案概要

目标：用 3 - 5 句话概括项目核心价值（参考格式：“在 [XXX 应用] 内交付 [YYY 能力]，解决 [痛点]，实现 [会话式问答/能力降级/可追溯] 等核心诉求”）。

【示例】：
在 投研问答助手 应用内，交付**“统一问答入口 + 关键信息验证”能力，解决投研人员“多系统信息分散、信息验证效率低”的痛点，实现会话式问答、能力降级（如 LLM 不可用时切换备用方案）、历史记录可追溯**的核心诉求，提升投研工作效率。

1. 范围边界

目标：明确“做什么（范围内）”和“不做什么（范围外）”，避免范围蔓延（各写 3 - 4 条）。

|做（范围内）|不做（范围外）|
| --- | --- |
|会话管理：创建、列表、删除会话|多租户/行级数据权限（仅支持单租户基础权限）|
|问答能力：基于 LLM 实现投研问题问答（支持文本/结构化数据问答）|复杂数据可视化（仅支持文本/表格类信息展示）|
|历史记录：会话历史存储、查询、导出|第三方系统深度集成（仅支持内部数据源对接）|

1. 成功标准（可度量）

目标：写出 3 - 4 条可量化的验收标准（句式参考：“用户在 X 分钟内完成 [XXX 全链路]” 或 “未配置外部模型时返回 [XXX 结果]”）。

|标准描述|度量方式|
| --- | --- |
|主路径可走通：用户完成“新建会话 → 提问 → 查看历史”全链路|用户在 3 分钟内完成该全链路操作|
|问答准确率：投研问题问答的准确率（人工标注验证）|准确率 ≥ 85%（基于 100 条典型问题的测试）|
|历史记录查询效率：用户查询历史会话的响应时间|响应时间 ≤ 2 秒（95% 场景下）|

1. 风险（简表）

目标：列出 2 - 3 个主要风险，分析影响并提出应对措施（常见方向：外部依赖不可用、数据存储并发、模块间契约延期等）。

|风险描述|影响|应对措施|
| --- | --- | --- |
|外部 LLM 不可用（如 API 限流、服务中断）|问答功能降级（无法调用外部模型）|三级降级：<|
|数据存储并发过高（多用户同时读写历史记录）|系统响应变慢、数据丢失/冲突|1. 优化数据库索引，提升读写性能；<|

1. 立项前置条件（Go / No - Go）

目标：明确项目启动前必须满足的条件（勾选或补充）。

- □ 后端与前端可独立启动（开发环境验证通过）
- □ 演示数据目录可读写（数据初始化、测试通过）
- □ 【补充其他前置条件】：如 “LLM 接口联调成功”“内部数据源对接完成”

版本管理

|版本|日期|说明|
| --- | --- | --- |
|v0.1|【填写日期】|首版填写（需求调研、方案设计阶段）|