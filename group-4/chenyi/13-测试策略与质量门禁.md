# 13 — 测试策略与质量门禁（引导版模板）

---

| 项 | 值 |
|---|---|
| 模块编号 | M1-QA |
| 模块名称 | 投研问答助手 |
| 文档版本 | v0.1 |
| 阶段 | Test（断言 — 怎样算通过） |
| 追溯文档 | `05` UserStory · `09` API 接口规格 · `14` 追踪矩阵 |

---

## 一、测试分层

> 共 4 层，写测试前先搞清楚每层验什么。

| 层级 | 工具 | 覆盖对象 | 说明 |
|------|------|----------|------|
| **L1 Unit** | pytest | 单个函数/类的逻辑 | 不依赖网络，最快 |
| **L2 Integration** | pytest + Flask test_client | API 端点 + 存储 | 模拟 HTTP 请求 |
| **L3 Contract** | pytest + schema 断言 | 响应体结构与 `09` 一致 | 验字段名、类型 |
| **L4 E2E** | Playwright / Cypress | 完整用户旅程 | 浏览器级操作 |

## 二、用例拆分原则

> **核心理念：数"可独立失败的点"** — 每条 TC 只回答一个问题，失败时不看代码就知道哪里坏了。

| 来源 | 计数方法 | 示例 |
|------|----------|------|
| 正常路径 | 每个端点的 happy path = 1 条 | `POST /ask → 200` = TC-001 |
| 错误码 | 每个 distinct error code = 1 条 | `EMPTY_QUERY` = TC-002 |
| 分支路径 | 每个 if/else 降级分支 = 1 条 | 无 API Key → demo = TC-004 |
| 类型约束 | Spec `09` 约定的字段类型 = 1 条 | `llm_used` 必须 bool = TC-006 |

## 三、★ 示例：已有测试用例

| TC-ID | 层级 | 测试函数 | 断言要点 | 关联 REQ |
|-------|------|----------|----------|----------|
| TC-M01-001 | Integration | `test_ask_success` | `POST /ask` → 200, 含 answer、llm_used、traceId | REQ-M1QA-002 |
| TC-M01-002 | Integration | `test_ask_empty_query` | 空 query → 400, `EMPTY_QUERY` | REQ-M1QA-002 |
| TC-M01-003 | Integration | `test_ask_long_query` | query > 500 → 400, `INVALID_QUERY` | REQ-M1QA-002 |
| TC-M01-004 | Integration | `test_bailian_fallback` | 无 API Key → `answer_source='demo'` | REQ-M1QA-002 |
| TC-M01-005 | Contract | `test_ask_response_schema` | `llm_used` 为 bool，`answer_source` 枚举值合法，`response_time_ms` 为非负整数 | REQ-M1QA-002 |
| TC-M01-006 | Integration | `test_ask_missing_session_id` | 缺少 `session_id` → 400, `INVALID_QUERY` | REQ-M1QA-002 |
| TC-M01-007 | Integration | `test_ask_invalid_session_id` | `session_id` 对应已删除会话 → 404, `SESSION_NOT_FOUND` | REQ-M1QA-002 |
| TC-M01-008 | Integration | `test_ask_copaw_fallback_to_bailian` | CoPaw 超时 → `answer_source='bailian'`，不抛异常 | REQ-M1QA-002 |
| TC-M01-009 | Integration | `test_ask_all_llm_unavailable` | CoPaw + 百炼均不可用 → `answer_source='demo'`，HTTP 200 | REQ-M1QA-002 |
| TC-M01-010 | Contract | `test_ask_cited_passages_structure` | `cited_passages` 为数组或 null；Demo 模式下必须为 null | REQ-M1QA-002 |

### 3.1 会话管理 API 测试

> 覆盖 `GET /sessions`、`POST /sessions`、`DELETE /sessions/<id>`。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-020 | Integration | `GET /sessions` → 200，返回 `sessions` 数组，空时为 `[]`，字段含 `session_id`、`title`、`created_at`、`query_count`、`status` | REQ-M1QA-001 | P0 |
| TC-M01-021 | Integration | `POST /sessions` → 201，响应含 `session_id`（UUID）、`title`（默认"新会话"）、`created_at`、`query_count=0`、`status=active` | REQ-M1QA-001 | P0 |
| TC-M01-022 | Integration | `POST /sessions` 传入自定义 `title` → 201，响应 `title` 与传入值一致 | REQ-M1QA-001 | P0 |
| TC-M01-023 | Integration | `POST /sessions` 传入超过 100 字符的 `title` → 400, `INVALID_QUERY` | REQ-M1QA-001 | P0 |
| TC-M01-024 | Integration | `DELETE /sessions/<id>` → 200，响应含 `status=deleted`、`deleted_at`；后续 `GET /sessions` 列表不再包含该会话 | REQ-M1QA-001 | P0 |
| TC-M01-025 | Integration | `DELETE /sessions/<id>` 目标 session 不存在 → 404, `SESSION_NOT_FOUND` | REQ-M1QA-001 | P0 |
| TC-M01-026 | Integration | `GET /sessions` 仅返回 `status=active` 会话，已删除会话不出现 | REQ-M1QA-001 | P0 |
| TC-M01-027 | Contract | `GET /sessions` 响应中 `sessions` 按 `created_at` 倒序排列 | REQ-M1QA-001 | P1 |

### 3.2 问答记录 API 测试

> 覆盖 `GET /sessions/<id>/records`，含有记录与无记录两种场景。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-030 | Integration | `GET /sessions/<id>/records`（有记录）→ 200，返回 `records` 数组，每条含 `record_id`、`query`、`answer`、`answer_source`、`cited_passages`、`created_at` | REQ-M1QA-003 | P0 |
| TC-M01-031 | Integration | `GET /sessions/<id>/records`（无记录）→ 200，`records` 为空数组 `[]` | REQ-M1QA-003 | P0 |
| TC-M01-032 | Integration | `GET /sessions/<id>/records` session 已删除 → 404, `SESSION_NOT_FOUND` | REQ-M1QA-003 | P0 |
| TC-M01-033 | Contract | `records` 按 `created_at` 正序排列；`created_at` 为合法 ISO-8601 UTC 格式 | REQ-M1QA-003 | P1 |
| TC-M01-034 | Integration | 新增问答后重新调用 `GET /sessions/<id>/records`，新记录出现在列表末尾，历史记录不丢失 | REQ-M1QA-003 | P0 |
| TC-M01-035 | Integration | 删除 Session 后，原关联 records 不再通过 `GET /sessions/<id>/records` 可访问（级联软删除验证） | REQ-M1QA-003 | P1 |

### 3.3 Storage 层单元测试

> 覆盖 Storage 类全部公开方法，对齐 `10` §5 方法清单。

| TC-ID | 层级 | 覆盖方法 | 断言要点 | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-040 | Unit | `Storage.__init__` | `DATA_DIR` 不存在时自动创建目录；`sessions.json` 与 `qa_records.json` 不存在时自动初始化为 `[]` | P0 |
| TC-M01-041 | Unit | `create_session` | 返回含 `session_id`、`title`、`created_at`、`query_count=0`、`status=active` 的完整对象；写入 `sessions.json` 后可通过 `get_sessions` 读取到 | P0 |
| TC-M01-042 | Unit | `get_sessions` | 仅返回 `status=active` 的会话；按 `created_at` 倒序；空文件时返回 `[]` | P0 |
| TC-M01-043 | Unit | `delete_session` | 目标 Session `status` 置为 `deleted`；关联 QARecord 全部从 `qa_records.json` 移除（级联删除）；`get_sessions` 不再返回该会话 | P0 |
| TC-M01-044 | Unit | `add_record` | 写入新 QARecord；同步更新对应 Session 的 `query_count +1` 与 `updated_at`；返回完整 record 对象 | P0 |
| TC-M01-045 | Unit | `get_records_by_session` | 返回指定 session 的全部记录，按 `created_at` 正序；会话不存在时返回 `[]` | P0 |
| TC-M01-046 | Unit | `add_record`（首次问答自动命名） | `query_count` 从 0→1 时，Session `title` 自动更新为 query 前 20 字符（超长追加 `...`）；第 2 次问答不再变更 title | P1 |
| TC-M01-047 | Unit | `delete_records_by_session` | 从 `qa_records.json` 移除所有匹配 `session_id` 的记录；不影响其他 session 的记录 | P1 |
| TC-M01-048 | Unit | `Storage.__init__`（文件损坏兜底） | `sessions.json` 内容非合法 JSON 时，以 `[]` 启动，写入 ERROR 日志，不抛异常，不覆盖损坏文件 | P1 |
| TC-M01-049 | Unit | `update_session` | 可更新 `title`、`updated_at`；`session_id`、`created_at` 不可变更（传入后保持原值）；返回更新后完整对象 | P1 |

### 3.4 健康检查测试

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-036 | Integration | `GET /capabilities` 正常状态 → 200，含 `status=ok`，各组件子项状态字段齐全，响应时间 ≤ 1000ms | REQ-M1QA-004 | P1 |
| TC-M01-037 | Integration | 模拟本地模型不可用时，`GET /capabilities` → 200，对应子项 `status=degraded`，整体接口不报错 | REQ-M1QA-004 | P1 |

### 3.5 E2E 主路径测试

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-050 | E2E | 种子数据 ≥ 10 条下，用户完成"新建会话 → 上传研报 → 提问 → 查看历史"全链路，耗时 ≤ 3 分钟，无阻断性错误 | 全部 REQ | P0 |
| TC-M01-051 | E2E | Demo 模式下主路径全程可走通，回答卡片显示"离线演示"灰色标签，不出现空白页或未处理异常 | REQ-M1QA-002 | P0 |

## 四、质量门禁

| Gate-ID | 条件 | 阻塞合并 |
|---------|------|----------|
| **G-LINT** | `black --check` + `flake8` 通过 | **是** |
| **G-UNIT** | Unit 测试全绿 | **是** |
| **G-INT** | Integration 测试全绿 | **是** |
| **G-AC** | P0 验收标准全覆盖（TC-M01-001～010、020～027、030～034、036、050～051） | **是** |
| **G-PERF** | `response_time_ms < 5000`（问答端点 P95） | 否（warning） |
| **G-SCHEMA** | Contract 测试（L3）全绿，响应体字段与 `09` 规格一致 | **是** |

### 执行命令

```bash
# 后端测试
cd groups/Group-X/NN/backend
python -m pytest tests/ -q

# 后端测试 + 覆盖率
python -m pytest tests/ -q --cov=. --cov-report=term-missing

# 仅跑 P0 用例
python -m pytest tests/ -q -m "p0"

# 仅跑 Storage 单元测试
python -m pytest tests/test_storage.py -v
```

## 五、TDD 适用场景

| 场景 | TDD 价值 | 建议 |
|------|----------|------|
| 简单 CRUD（列表、删除） | 低 | 写完补测试 |
| 参数校验 / 错误码 | 中 | 建议 TDD |
| 多级降级 / 状态机 | **高** | **强烈建议 TDD** |
| 金融计算 / 风险判定 | **极高** | **必须 TDD** |

> 本模块重点推荐 TDD 场景：W3 三级降级链（TC-M01-004、008、009）、Storage 级联删除（TC-M01-043、047）、首次问答自动命名（TC-M01-046）。

## 六、追溯关系

> 汇总所有 TC 与 REQ 的映射，完成后同步到 `14` 追踪矩阵。

| 需求编号 | 已实现 TC | 待补充 TC |
|----------|-----------|-----------|
| REQ-M1QA-001 | TC-M01-020～027、TC-M01-041～043 | TC-M01-050（E2E 会话侧栏） |
| REQ-M1QA-002 | TC-M01-001～010 | TC-M01-050～051（E2E 问答全链路） |
| REQ-M1QA-003 | TC-M01-030～035、TC-M01-044～045 | TC-M01-050（E2E 历史查看） |
| REQ-M1QA-004 | TC-M01-036～037 | — |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-04-14 | 首版填写 |