# 13 — 测试策略与质量门禁（引导版模板）

---

| 项 | 值 |
|---|---|
| 模块编号 | M4-RA |
| 模块名称 | 研报聚合分析助手 |
| 文档版本 | v0.1 |
| 阶段 | Test（断言 — 怎样算通过） |
| 追溯文档 | `05` UserStory · `09` API 接口规格 · `14` 追踪矩阵 |

---

## 一、测试分层

> 共 4 层，写测试前先搞清楚每层验什么。

| 层级 | 工具 | 覆盖对象 | 说明 |
|------|------|----------|------|
| **L1 Unit** | pytest | 单个函数/类的逻辑 | 不依赖网络，最快 |
| **L2 Integration** | pytest + Flask test_client | API 端点 + 存储 | 模拟 HTTP 请求 |
| **L3 Contract** | pytest + schema 断言 | 响应体结构与 `09` 一致 | 验字段名、类型 |
| **L4 E2E** | Playwright / Cypress | 完整用户旅程 | 浏览器级操作 |

## 二、用例拆分原则

> **核心理念：数"可独立失败的点"** — 每条 TC 只回答一个问题，失败时不看代码就知道哪里坏了。

| 来源 | 计数方法 | 示例 |
|------|----------|------|
| 正常路径 | 每个端点的 happy path = 1 条 | `POST /ask → 200` = TC-001 |
| 错误码 | 每个 distinct error code = 1 条 | `EMPTY_QUERY` = TC-002 |
| 分支路径 | 每个 if/else 降级分支 = 1 条 | 无 API Key → demo = TC-004 |
| 类型约束 | Spec `09` 约定的字段类型 = 1 条 | `llm_used` 必须 bool = TC-006 |

## 三、★ 示例：已有测试用例

| TC-ID | 层级 | 测试函数 | 断言要点 | 关联 REQ |
|-------|------|----------|----------|----------|
| TC-M01-001 | Integration | `test_ask_success` | `POST /ask` → 200, 含 answer、llm_used、traceId | REQ-001 |
| TC-M01-002 | Integration | `test_ask_empty_query` | 空 query → 400, `EMPTY_QUERY` | REQ-005 |
| TC-M01-003 | Integration | `test_ask_long_query` | query >500 → 400, `INVALID_QUERY` | REQ-005 |
| TC-M01-004 | Integration | `test_bailian_fallback` | 无 API Key → `answer_source='demo'` | REQ-003 |

### 3.1 会话管理 API 测试

> 覆盖 GET /sessions、POST /sessions、PUT /sessions/<id>、DELETE /sessions/<id>。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-020 | Integration | GET /sessions → 200, 返回 sessions 数组，按时间倒序 | REQ-M4RA-001 | P0 |
| TC-M01-021 | Integration | POST /sessions → 201, 含 session_id、title、created_at | REQ-M4RA-001 | P0 |
| TC-M01-022 | Integration | DELETE /sessions/<id> → 200, 级联删除关联 qa_records | REQ-M4RA-001 | P0 |
| TC-M01-023 | Integration | DELETE 不存在的 session → 404, `SESSION_NOT_FOUND` | REQ-M4RA-001 | P0 |
| TC-M01-024 | Integration | PUT /sessions/<id> → 200, title 更新成功 | REQ-M4RA-001 | P1 |
| TC-M01-025 | Integration | POST /sessions title 超 100 字符 → 400, `INVALID_QUERY` | REQ-M4RA-001 | P1 |

### 3.2 问答记录 API 测试

> 覆盖 GET /sessions/<id>/records。有记录和无记录两种场景。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-030 | Integration | GET /sessions/<id>/records → 200, 有记录时返回 records 数组，含 query/answer/answer_source/timestamp | REQ-M4RA-002 | P0 |
| TC-M01-031 | Integration | GET /sessions/<id>/records → 200, 无记录时返回空数组 | REQ-M4RA-002 | P1 |
| TC-M01-032 | Integration | GET /sessions/<id>/records 不存在的 session → 404, `SESSION_NOT_FOUND` | REQ-M4RA-002 | P0 |

### 3.3 研报管理 API 测试

> 覆盖 POST /reports、GET /reports、GET /reports/<id>、DELETE /reports/<id>、PUT /reports/<id>/mark、POST /reports/<id>/parse、POST /reports/compare。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-048 | Integration | POST /reports 上传 PDF → 201, 含 report_id、status=pending | REQ-M4RA-003 | P0 |
| TC-M01-049 | Integration | POST /reports 上传非 PDF/HTML → 400, `INVALID_FILE_TYPE` | REQ-M4RA-003 | P0 |
| TC-M01-050 | Integration | GET /reports → 200, 返回 reports 数组，支持 keyword/rating 筛选 | REQ-M4RA-006 | P0 |
| TC-M01-051 | Integration | GET /reports/<id> → 200, 含 parsed_result（status=completed 时） | REQ-M4RA-003 | P0 |
| TC-M01-052 | Integration | DELETE /reports/<id> → 200, 级联删除解析结果 | REQ-M4RA-003 | P1 |
| TC-M01-053 | Integration | PUT /reports/<id>/mark → 200, mark_status 更新成功 | REQ-M4RA-006 | P1 |
| TC-M01-054 | Integration | POST /reports/<id>/parse → 200, 解析结果含 rating/target_price/core_views | REQ-M4RA-003 | P0 |
| TC-M01-055 | Integration | POST /reports/<id>/parse 解析耗时 < 5000ms | REQ-M4RA-003 | P0 |
| TC-M01-056 | Integration | POST /reports/compare 2-10份研报 → 200, 含 comparison 对比表 | REQ-M4RA-004 | P0 |
| TC-M01-057 | Integration | POST /reports/compare 研报数 <2 → 400, `INVALID_REPORT_SELECTION` | REQ-M4RA-004 | P0 |

### 3.4 Storage 层单元测试

> 覆盖 Storage 类核心方法：会话 CRUD、记录 CRUD、研报 CRUD、解析结果、对比。

| TC-ID | 层级 | 覆盖方法 | 断言要点 | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-040 | Unit | `Storage.__init__` | 目录不存在时自动创建，JSON 文件初始化为空列表 | P0 |
| TC-M01-041 | Unit | `create_session` | 创建会话返回 dict，含 session_id/title/created_at/query_count=0 | P0 |
| TC-M01-042 | Unit | `get_sessions` | 返回全部会话列表，按 created_at 倒序 | P0 |
| TC-M01-043 | Unit | `delete_session` | 删除会话 + 级联删除关联 qa_records | P0 |
| TC-M01-044 | Unit | `add_record` | 写入记录 + query_count 自动 +1 | P0 |
| TC-M01-045 | Unit | `get_records_by_session` | 按 session_id 过滤，无记录返回空列表 | P0 |
| TC-M01-046 | Unit | `update_session` | 更新标题成功，updated_at 刷新 | P1 |
| TC-M01-047 | Unit | `delete_records_by_session` | 删除全部记录，返回删除数量 | P1 |
| TC-M01-048s | Unit | `create_report` | 创建研报记录，初始 status=pending | P0 |
| TC-M01-049s | Unit | `get_reports` | 分页查询，支持 keyword/rating 筛选 | P0 |
| TC-M01-050s | Unit | `save_parse_result` | 保存解析结果，含 rating/target_price/core_views | P0 |
| TC-M01-051s | Unit | `compare_reports` | 生成对比数据，含 headers + rows | P0 |

## 四、质量门禁

| Gate-ID | 条件 | 阻塞合并 |
|---------|------|----------|
| **G-LINT** | `black --check` + `flake8` 通过 | **是** |
| **G-UNIT** | Unit 测试全绿 | **是** |
| **G-INT** | Integration 测试全绿 | **是** |
| **G-AC** | P0 验收标准全覆盖 | **是** |
| **G-PERF** | `response_time_ms < 5000` | 否（warning） |

### 执行命令

```bash
# 后端测试
cd groups/Group-X/NN/backend
python -m pytest tests/ -q

# 后端测试 + 覆盖率
python -m pytest tests/ -q --cov=. --cov-report=term-missing
```

## 五、TDD 适用场景

| 场景 | TDD 价值 | 建议 |
|------|----------|------|
| 简单 CRUD（列表、删除） | 低 | 写完补测试 |
| 参数校验 / 错误码 | 中 | 建议 TDD |
| 多级降级 / 状态机 | **高** | **强烈建议 TDD** |
| 金融计算 / 风险判定 | **极高** | **必须 TDD** |

## 六、追溯关系

> 汇总所有 TC 与 REQ 的映射，完成后同步到 `14` 追踪矩阵。

| 需求编号 | 已实现 TC | 待补充 TC |
|----------|-----------|----------|
| REQ-M4RA-001 | TC-M01-020～025 | — |
| REQ-M4RA-002 | TC-M01-001～004, TC-M01-030～032 | — |
| REQ-M4RA-003 | TC-M01-048～055 | — |
| REQ-M4RA-004 | TC-M01-056～057 | — |
| REQ-M4RA-005 | TC-M01-001 (部分) | TC-M01-060～065（研报智能问答专项） |
| REQ-M4RA-006 | TC-M01-050, 053 | — |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-04-14 | 首版填写，基于 05/09/10 文档完成 |
