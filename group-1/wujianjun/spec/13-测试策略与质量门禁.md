# 13 — 测试策略与质量门禁

---

| 项 | 值 |
|---|---|
| 模块编号 | M1-IRA |
| 模块名称 | 投研智能问答助手 |
| 文档版本 | v0.1 |
| 阶段 | Test（How — 质量验证） |
| 上游 | `05` 验收标准 · `09` API 规格 · `10` 数据模型 · `12` 实施计划 |
| 下游 | → `14` 需求追踪矩阵 · 测试执行 |

---

> **13 只做一件事**：定义**测试策略、测试用例、质量门禁**，确保交付物符合 `05` 验收标准。

---

## 1. 测试策略总览

### 1.1 测试分层金字塔

```
        /\
       /  \  E2E 测试（主路径）
      /____\     TC-M01-100~102
     /      \
    /   集成  \  API 集成测试
   /   测试    \   TC-M01-040~099
  /______________\
 /                \
/    单元测试       \ 函数/类级别测试
____________________   TC-M01-001~039
```

### 1.2 测试类型与工具

| 测试类型 | 工具 | 覆盖目标 | 执行时机 |
|----------|------|----------|----------|
| 单元测试 | pytest | 后端函数/类，覆盖率 ≥ 80% | 每次提交 |
| 集成测试 | pytest + requests | API 端点，6 个端点全覆盖 | M2 完成时 |
| E2E 测试 | Playwright / 手工 | 主路径 3 分钟演示 | M3 完成时 |
| 性能测试 | locust / 脚本 | P95 < 5s | M4-T02 |
| 安全测试 | bandit + 代码扫描 | 无密钥硬编码 | M4-T03 |

---

## 2. 质量门禁（5 Gate）

### Gate 1: 代码准入（提交前）

| 检查项 | 通过阈值 | 检查方式 | 失败处理 |
|--------|----------|----------|----------|
| 代码编译 | 0 语法错误 | `python -m py_compile **/*.py` | 阻断提交 |
| 代码规范 | PEP 8 违规 ≤ 5 | `flake8 --max-line-length=100` | 阻断提交 |
| 单元测试 | 通过率 100% | `pytest tests/unit/ -x` | 阻断提交 |

### Gate 2: 集成准入（合入前）

| 检查项 | 通过阈值 | 检查方式 | 失败处理 |
|--------|----------|----------|----------|
| 单元测试覆盖率 | ≥ 80% | `pytest --cov=src --cov-report=term-missing` | 阻断合入 |
| 集成测试 | 通过率 100% | `pytest tests/integration/ -v` | 阻断合入 |
| API 契约 | 6 个端点全部通过 | 自动测试 09 所有端点 | 阻断合入 |

### Gate 3: 功能准出（M3 结束）

| 检查项 | 通过阈值 | 检查方式 | 失败处理 |
|--------|----------|----------|----------|
| E2E 主路径 | 3 分钟演示完成 | Playwright / 手工 | 延期交付 |
| AC 覆盖率 | 05 全部 15 条 AC 通过 | 对照 14 追踪矩阵验证 | 延期交付 |
| 降级验证 | Demo 模式 100% 可用 | 无密钥环境测试 | 延期交付 |

### Gate 4: 性能准出（M4-T02）

| 检查项 | 通过阈值 | 检查方式 | 失败处理 |
|--------|----------|----------|----------|
| 问答延迟 P95 | < 5000ms | locust 100 次 POST /ask | 优化后重测 |
| 会话列表延迟 P95 | < 500ms | locust 100 次 GET /sessions | 优化后重测 |
| 内存泄漏 | 1 小时运行无增长 | 持续监控内存占用 | 优化后重测 |

### Gate 5: 安全准出（M4-T03）

| 检查项 | 通过阈值 | 检查方式 | 失败处理 |
|--------|----------|----------|----------|
| 密钥硬编码 | 0 处命中 | `bandit -r . -ll` + 人工审计 | 阻断发布 |
| 高危漏洞 | 0 个 CVE | `pip audit` + `npm audit` | 阻断发布 |
| 依赖许可证 | 无 GPL 污染 | `pip-licenses` 扫描 | 阻断发布 |
| 日志脱敏 | API Key 无明文 | 正则扫描日志输出 | 阻断发布 |

---

## 3. 测试用例清单

### 3.1 单元测试 — Storage 层（TC-M01-001~010）

对齐 `10` §5 Storage 方法清单

| 用例编号 | 测试目标 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|----------|----------|----------|----------|----------|--------|
| TC-M01-001 | `create_session` 创建会话 | 无 | 调用 `create_session("sess_001", "测试会话")` | 返回 Session 对象，字段完整 | P0 |
| TC-M01-002 | `create_session` 默认标题 | 无 | 调用 `create_session("sess_002")` | 返回 title="新会话" | P0 |
| TC-M01-003 | `get_sessions` 列表排序 | 已创建 3 个会话 | 调用 `get_sessions()` | 按 created_at 倒序排列 | P0 |
| TC-M01-004 | `delete_session` 删除 | 存在 sess_001 | 调用 `delete_session("sess_001")` | 返回删除记录数 1 | P0 |
| TC-M01-005 | `delete_session` 级联删除 | sess_001 下有 2 条记录 | 调用 `delete_session("sess_001")` | 会话和记录均被删除 | P0 |
| TC-M01-006 | `update_session` 更新 | 存在 sess_001 | 调用 `update_session("sess_001", {"title": "新标题"})` | 返回更新后的 Session | P0 |
| TC-M01-007 | `add_record` 添加记录 | 存在 sess_001 | 调用 `add_record` 传入完整参数 | 返回 QARecord 对象，session.query_count +1 | P0 |
| TC-M01-008 | `get_records_by_session` 查询 | sess_001 下有 3 条记录 | 调用 `get_records_by_session("sess_001")` | 返回 3 条记录，按时间正序 | P0 |
| TC-M01-009 | `delete_records_by_session` 删除 | sess_001 下有 2 条记录 | 调用 `delete_records_by_session("sess_001")` | 返回 2，记录被删除 | P0 |
| TC-M01-010 | 首次问答自动命名 | sess_001 query_count=0 | 添加第一条记录 | session.title 自动更新为 query 前 20 字 | P0 |

### 3.2 单元测试 — Agent 层（TC-M01-011~020）

对齐 `08` §4 三级降级编排

| 用例编号 | 测试目标 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|----------|----------|----------|----------|----------|--------|
| TC-M01-011 | CoPaw 正常返回 | CoPaw 配置正确 | 调用 `ask(query, session_id)` | answer_source='copaw', llm_used=true | P0 |
| TC-M01-012 | CoPaw 降级到百炼 | CoPaw 不可用 | 调用 `ask(query, session_id)` | 自动降级，answer_source='bailian' | P0 |
| TC-M01-013 | 百炼降级到 Demo | 两者都不可用 | 调用 `ask(query, session_id)` | 自动降级，answer_source='demo', llm_used=false | P0 |
| TC-M01-014 | Demo 模式始终可用 | 无 API 密钥 | 调用 `ask(query, session_id)` | 返回 Demo 答案，不抛异常 | P0 |
| TC-M01-015 | 响应时间记录 | 任意模式 | 调用 `ask(query, session_id)` | response_time_ms > 0 且 < 5000 | P0 |
| TC-M01-016 | 三级降级不跳级 | CoPaw 不可用但百炼可用 | 调用 `ask(query, session_id)` | 先到 CoPaw 失败，再到百炼成功 | P0 |
| TC-M01-017 | 问答记录持久化 | 任意模式 | 调用 `ask(query, session_id)` | QARecord 被写入 storage | P0 |
| TC-M01-018 | session 时间戳更新 | 存在 sess_001 | 调用 `ask(query, "sess_001")` | session.updated_at 被更新 | P0 |
| TC-M01-019 | query_count 递增 | sess_001 query_count=2 | 调用 `ask(query, "sess_001")` | session.query_count=3 | P0 |
| TC-M01-020 | 无效 session_id 处理 | session_id 不存在 | 调用 `ask(query, "invalid_id")` | 抛出 SESSION_NOT_FOUND 错误 | P0 |

### 3.3 集成测试 — API 端点（TC-M01-021~040）

对齐 `09` API 规格

#### 3.3.1 会话管理 API（TC-M01-021~028）

| 用例编号 | 测试目标 | 请求 | 预期响应 | 优先级 |
|----------|----------|------|----------|--------|
| TC-M01-021 | POST /sessions 创建 | `{"title": "测试"}` | 201, 返回完整 Session | P0 |
| TC-M01-022 | POST /sessions 默认标题 | `{}` | 201, title="新会话" | P0 |
| TC-M01-023 | POST /sessions 标题过长 | `{"title": "...101字..."}` | 400, INVALID_QUERY | P0 |
| TC-M01-024 | GET /sessions 列表 | GET /sessions | 200, sessions[] 倒序 | P0 |
| TC-M01-025 | GET /sessions 分页 | GET /sessions?limit=2 | 200, 返回 2 条 | P1 |
| TC-M01-026 | DELETE /sessions/{id} 删除 | DELETE /sessions/sess_001 | 200, 返回删除确认 | P0 |
| TC-M01-027 | DELETE /sessions/{id} 不存在 | DELETE /sessions/invalid | 404, SESSION_NOT_FOUND | P0 |
| TC-M01-028 | DELETE /sessions/{id} 级联删除 | sess_001 有 3 条记录 | 200, deleted_records_count=3 | P0 |

#### 3.3.2 问答 API（TC-M01-029~035）

| 用例编号 | 测试目标 | 请求 | 预期响应 | 优先级 |
|----------|----------|------|----------|--------|
| TC-M01-029 | POST /ask 正常提问 | `{"query": "测试", "session_id": "sess_001"}` | 200, 返回完整 answer + 元数据 | P0 |
| TC-M01-030 | POST /ask 空 query | `{"query": "", "session_id": "sess_001"}` | 400, EMPTY_QUERY | P0 |
| TC-M01-031 | POST /ask query 超长 | `{"query": "...501字...", "session_id": "sess_001"}` | 400, INVALID_QUERY | P0 |
| TC-M01-032 | POST /ask 无效 session_id | `{"query": "测试", "session_id": "invalid"}` | 400, SESSION_NOT_FOUND | P0 |
| TC-M01-033 | POST /ask 返回 record_id | 正常请求 | 200, data.record_id 存在 | P0 |
| TC-M01-034 | POST /ask 响应时间 | 正常请求 | 200, response_time_ms < 5000 | P0 |
| TC-M01-035 | POST /ask traceId | 正常请求 | 200, traceId 存在且格式正确 | P0 |

#### 3.3.3 历史记录与能力 API（TC-M01-036~040）

| 用例编号 | 测试目标 | 请求 | 预期响应 | 优先级 |
|----------|----------|------|----------|--------|
| TC-M01-036 | GET /sessions/{id}/records 查询 | GET /sessions/sess_001/records | 200, records[] 正序 | P0 |
| TC-M01-037 | GET /sessions/{id}/records 空 | 新会话无记录 | 200, records=[], total=0 | P0 |
| TC-M01-038 | GET /sessions/{id}/records 无效ID | GET /sessions/invalid/records | 404, SESSION_NOT_FOUND | P0 |
| TC-M01-039 | GET /capabilities 能力探测 | GET /capabilities | 200, 返回配置状态 | P0 |
| TC-M01-040 | GET /capabilities traceId | GET /capabilities | 200, traceId 存在 | P0 |

### 3.4 E2E 测试 — 主路径（TC-M01-041~045）

对齐 `04` §6 可演示标准

| 用例编号 | 测试目标 | 测试步骤 | 预期结果 | 优先级 |
|----------|----------|----------|----------|--------|
| TC-M01-041 | 完整主路径 | 1. 打开页面<br>2. 创建新会话<br>3. 输入问题<br>4. 发送<br>5. 查看回答<br>6. 查看历史 | 3 分钟内完成，无错误 | P0 |
| TC-M01-042 | 离线模式体验 | 1. 清除 API 密钥<br>2. 刷新页面<br>3. 提问 | 显示"离线演示"，正常返回答案 | P0 |
| TC-M01-043 | 会话管理流程 | 1. 创建 3 个会话<br>2. 删除中间会话<br>3. 验证级联删除 | 会话和记录正确删除 | P0 |
| TC-M01-044 | 错误提示验证 | 1. 空输入发送<br>2. 超长输入发送 | 正确显示 Toast 错误提示 | P0 |
| TC-M01-045 | 能力状态切换 | 1. 配置百炼密钥<br>2. 刷新页面 | Header 显示"百炼"状态 | P0 |

### 3.5 性能测试（TC-M01-046~050）

对齐 `07` §1.1 延迟 SLO

| 用例编号 | 测试目标 | 测试方法 | 预期结果 | 优先级 |
|----------|----------|----------|----------|--------|
| TC-M01-046 | 问答 P95 延迟 | 100 次 POST /ask | P95 < 5000ms | P0 |
| TC-M01-047 | 会话列表延迟 | 100 次 GET /sessions | P95 < 500ms | P0 |
| TC-M01-048 | 并发请求处理 | 10 并发 × 10 次 | 无 500 错误 | P1 |
| TC-M01-049 | 大数据量列表 | 100 个会话 | GET /sessions < 500ms | P1 |
| TC-M01-050 | 内存占用 | 持续运行 1 小时 | 内存无泄漏 | P1 |

---

## 4. 追溯关系表

### 4.1 AC → TC 对齐（与 05 验收标准）

| 验收标准 | 对应测试用例 | 验证方式 |
|----------|--------------|----------|
| AC-001-01 创建会话 | TC-M01-021, TC-M01-022 | 集成测试 |
| AC-001-02 会话列表 | TC-M01-024 | 集成测试 |
| AC-001-03 删除会话 | TC-M01-026, TC-M01-028 | 集成测试 |
| AC-001-04 自动命名 | TC-M01-010 | 单元测试 |
| AC-002-01 正常问答 | TC-M01-029, TC-M01-033~035 | 集成测试 |
| AC-002-02 EMPTY_QUERY | TC-M01-030 | 集成测试 |
| AC-002-03 INVALID_QUERY | TC-M01-031 | 集成测试 |
| AC-002-04 Demo 降级 | TC-M01-014, TC-M01-042 | 单元测试 + E2E |
| AC-002-05 P95 < 5s | TC-M01-046 | 性能测试 |
| AC-003-01 查看历史 | TC-M01-036 | 集成测试 |
| AC-003-02 记录展示 | TC-M01-041 | E2E 测试 |
| AC-003-03 来源标签 | TC-M01-045 | E2E 测试 |
| AC-004-01 能力状态 | TC-M01-039, TC-M01-045 | 集成测试 + E2E |
| AC-004-02 本地存储 | TC-M01-005 | 单元测试 |
| AC-004-03 环境变量 | Gate 5 安全审计 | 代码扫描 |

### 4.2 AC → TC → API 端点全链路覆盖

| API 端点 (09) | 方法 | 覆盖的 AC | 对应 TC | 测试层级 |
|---------------|------|-----------|---------|----------|
| `/capabilities` | GET | AC-004-01 | TC-M01-039, TC-M01-040 | L3 API |
| `/ask` | POST | AC-002-01~05 | TC-M01-029~035, TC-M01-046 | L3 API + L4 性能 |
| `/sessions` | GET | AC-001-02 | TC-M01-024, TC-M01-025 | L3 API |
| `/sessions` | POST | AC-001-01, AC-001-04 | TC-M01-021~023 | L3 API |
| `/sessions/{id}` | DELETE | AC-001-03 | TC-M01-026~028 | L3 API |
| `/sessions/{id}/records` | GET | AC-003-01~03 | TC-M01-036~038 | L3 API |

> **覆盖验证**：09 全部 6 个 API 端点均已通过集成测试（L3）覆盖，覆盖率 100%。

---

## 5. 测试执行计划

| 阶段 | 时间 | 执行测试 | 负责人 |
|------|------|----------|--------|
| M1 结束 | Week 1 周五 | TC-M01-001~005（Storage） | 后端 |
| M2 结束 | Week 2 周五 | TC-M01-001~040（全部单元+集成） | 测试 |
| M3 结束 | Week 3 周五 | TC-M01-041~045（E2E） | 全栈 |
| M4 结束 | Week 4 周五 | TC-M01-046~050（性能）+ 全量回归 | 测试 |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-04-14 | 基于02-11文档生成首版 |
