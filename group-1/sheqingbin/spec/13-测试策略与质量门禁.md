# 13 — 测试策略与质量门禁（引导版模板）

---

| 项 | 值 |
|---|---|
| 模块编号 | M1-QA |
| 模块名称 | 投研问答助手 |
| 文档版本 | v0.1 |
| 阶段 | Test（断言 — 怎样算通过） |
| 追溯文档 | `05` UserStory · `09` API 接口规格 · `14` 追踪矩阵 |

---

## 一、测试分层

> 共 4 层，写测试前先搞清楚每层验什么。

| 层级 | 工具 | 覆盖对象 | 说明 |
|------|------|----------|------|
| **L1 Unit** | pytest | 单个函数/类的逻辑 | 不依赖网络，最快 |
| **L2 Integration** | pytest + Flask test_client | API 端点 + 存储 | 模拟 HTTP 请求 |
| **L3 Contract** | pytest + schema 断言 | 响应体结构与 `09` 一致 | 验字段名、类型 |
| **L4 E2E** | Playwright / Cypress | 完整用户旅程 | 浏览器级操作 |

## 二、用例拆分原则

> **核心理念：数"可独立失败的点"** — 每条 TC 只回答一个问题，失败时不看代码就知道哪里坏了。

| 来源 | 计数方法 | 示例 |
|------|----------|------|
| 正常路径 | 每个端点的 happy path = 1 条 | `POST /ask → 200` = TC-001 |
| 错误码 | 每个 distinct error code = 1 条 | `EMPTY_QUERY` = TC-002 |
| 分支路径 | 每个 if/else 降级分支 = 1 条 | 无 API Key → demo = TC-004 |
| 类型约束 | Spec `09` 约定的字段类型 = 1 条 | `llm_used` 必须 bool = TC-006 |

## 三、★ 示例：已有测试用例

| TC-ID | 层级 | 测试函数 | 断言要点 | 关联 REQ |
|-------|------|----------|----------|----------|
| TC-M01-001 | Integration | `test_ask_success` | `POST /ask` → 200, 含 answer、llm_used、traceId | REQ-001 |
| TC-M01-002 | Integration | `test_ask_empty_query` | 空 query → 400, `EMPTY_QUERY` | REQ-005 |
| TC-M01-003 | Integration | `test_ask_long_query` | query >500 → 400, `INVALID_QUERY` | REQ-005 |
| TC-M01-004 | Integration | `test_bailian_fallback` | 无 API Key → `answer_source='demo'` | REQ-003 |

### 3.1 会话管理 API 测试

> 覆盖 GET /sessions、POST /sessions、DELETE /sessions/<id>。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-020 | Integration | GET /sessions → 200, 返回 sessions 数组，含 session_id/title/created_at/updated_at/query_count | REQ-001 | P0 |
| TC-M01-021 | Integration | POST /sessions → 201, 返回 session_id/title/created_at/updated_at/query_count，title 默认"新会话" | REQ-001 | P0 |
| TC-M01-022 | Integration | DELETE /sessions/<id> → 200, 返回 success=true，级联删除关联 QARecord | REQ-001 | P0 |
| TC-M01-023 | Integration | DELETE /sessions/<invalid_id> → 404, error.code=SESSION_NOT_FOUND | REQ-001 | P1 |
| TC-M01-024 | Integration | POST /sessions 带 title → 201, 返回指定 title，title ≤23 字符 | REQ-001 | P0 |

### 3.2 问答记录 API 测试

> 覆盖 GET /sessions/<id>/records。有记录和无记录两种场景。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-030 | Integration | GET /sessions/<id>/records → 200, 返回 records 数组，含 id/query/answer/llm_used/model/response_time_ms/answer_source/timestamp | REQ-003 | P0 |
| TC-M01-031 | Integration | GET /sessions/<id>/records 无记录时 → 200, 返回空数组 records=[] | REQ-003 | P1 |
| TC-M01-032 | Integration | GET /sessions/<invalid_id>/records → 404, error.code=SESSION_NOT_FOUND | REQ-003 | P1 |

### 3.3 Storage 层单元测试

> 覆盖 Storage 类核心方法：`__init__`、`create_session`、`get_sessions`、`delete_session`（级联）、`add_record`、`get_records_by_session`。

| TC-ID | 层级 | 覆盖方法 | 断言要点 | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-040 | Unit | `Storage.__init__` | 目录不存在时自动创建 data_dir，初始化 sessions.json 和 qa_records.json | P0 |
| TC-M01-041 | Unit | `create_session` | 创建新会话，返回 session_id/title/created_at/updated_at/query_count，title 默认"新会话" | P0 |
| TC-M01-042 | Unit | `get_sessions` | 返回全部会话列表，按 updated_at 降序排列 | P0 |
| TC-M01-043 | Unit | `delete_session` | 删除指定会话，级联删除关联 QARecord，返回 None | P0 |
| TC-M01-044 | Unit | `add_record` | 写入问答记录，更新对应 Session 的 query_count +1，返回记录对象 | P0 |
| TC-M01-045 | Unit | `get_records_by_session` | 按 session_id 过滤返回 QARecord 列表，按 timestamp 升序排列 | P0 |
| TC-M01-046 | Unit | `update_session` | 更新会话 title 和 updated_at，返回更新后的会话对象 | P1 |
| TC-M01-047 | Unit | `delete_records_by_session` | 删除指定会话的所有 QARecord，返回实际删除条数 | P1 |

## 四、质量门禁

| Gate-ID | 条件 | 阈值/标准 | 阻塞合并 |
|---------|------|-----------|----------|
| **G-LINT** | `black --check` + `flake8` 通过 | 代码格式零警告，PEP8 合规 | **是** |
| **G-UNIT** | Unit 测试全绿 | 通过率 100%，TC-M01-040~047 全部通过 | **是** |
| **G-INT** | Integration 测试全绿 | 通过率 100%，TC-M01-001~004、020~024、030~032 全部通过 | **是** |
| **G-AC** | P0 验收标准全覆盖 | 05 中所有 P0 级 AC（AC-001-01~03、AC-002-01~03、AC-003-01~02）均有对应 TC | **是** |
| **G-PERF** | 响应时间达标 | response_time_ms < 5000ms（P0 场景），>5000ms 标记为 warning | 否（warning） |

### 执行命令

```bash
# 后端测试
cd groups/Group-X/NN/backend
python -m pytest tests/ -q

# 后端测试 + 覆盖率
python -m pytest tests/ -q --cov=. --cov-report=term-missing
```

## 五、TDD 适用场景

| 场景 | TDD 价值 | 建议 |
|------|----------|------|
| 简单 CRUD（列表、删除） | 低 | 写完补测试 |
| 参数校验 / 错误码 | 中 | 建议 TDD |
| 多级降级 / 状态机 | **高** | **强烈建议 TDD** |
| 金融计算 / 风险判定 | **极高** | **必须 TDD** |

## 六、追溯关系

> 汇总所有 TC 与 REQ 的映射，完成后同步到 `14` 追踪矩阵。

| 需求编号 | UserStory | 已实现 TC | 覆盖 AC | 关联 API |
|----------|-----------|-----------|---------|----------|
| REQ-001 | 会话管理 | TC-M01-020, 021, 022, 023, 024, 040~043, 046 | AC-001-01, AC-001-02, AC-001-03 | GET /sessions, POST /sessions, DELETE /sessions/<id> |
| REQ-002 | 问答提交 | TC-M01-001, 002, 003, 004 | AC-002-01, AC-002-02, AC-002-03 | POST /ask |
| REQ-003 | 历史记录 | TC-M01-004, 030, 031, 032, 044, 045, 047 | AC-003-01, AC-003-02 | GET /sessions/<id>/records, POST /ask |
| REQ-005 | 参数校验 | TC-M01-002, 003 | - | POST /ask |

### AC → TC → API 端点全链路覆盖

| AC 编号 | AC 描述 | 对应 TC | 关联 API 端点 |
|---------|---------|---------|---------------|
| AC-001-01 | 左侧显示会话列表，可删除会话 | TC-M01-020, 022, 023 | GET /sessions, DELETE /sessions/<id> |
| AC-001-02 | 提交会话后左侧显示新会话 | TC-M01-021 | POST /sessions |
| AC-001-03 | 点击左侧会话显示会话内容 | TC-M01-030, 031 | GET /sessions/<id>/records |
| AC-002-01 | 已有会话提问不新增会话 | TC-M01-001 | POST /ask |
| AC-002-02 | 新会话提问新增会话 | TC-M01-021, 001 | POST /sessions, POST /ask |
| AC-002-03 | 会话中断支持继续回答 | TC-M01-004 | POST /ask |
| AC-003-01 | AI 识别意图给出对比表 | TC-M01-001, 004 | POST /ask |
| AC-003-02 | 实时解析和对比 | TC-M01-004 | POST /ask |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2025-04-14 | 首版填写 |
