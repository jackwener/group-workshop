# 15 — 实施任务清单

---

| 项 | 值 |
|---|---|
| 模块编号 | M1-QA |
| 模块名称 | 投研问答助手 |
| 文档版本 | v0.1 |
| 阶段 | Task（基于 Spec 拆解的开发任务） |
| 上游 | `08` 架构 · `09` API · `10` 数据 · `12` 里程碑 · `13` 测试策略 |

---

> **任务编号规则**：`TASK-S{阶段}-W{WBS编号}-{序号}`
> **状态**：`TODO` / `IN_PROGRESS` / `DONE` / `BLOCKED`
> **对齐**：每条任务必须关联 REQ、WBS、TC

---

## 〇、项目初始化

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-S0-W0-001 | 初始化后端项目结构 | 创建 `backend/` 目录结构：`app/api/`、`app/services/`、`app/models/`、`app/dao/`、`app/utils/`、`data/sessions/`、`data/analysis/`、`data/reports/`、`data/uploads/`、`tests/unit/`、`tests/integration/` | `08` §2.1 | 目录结构与架构文档一致，Flask app 可启动 | TODO |
| TASK-S0-W0-002 | 初始化前端项目结构 | 使用 Vite 创建 React 项目，目录含 `src/components/`、`src/pages/`、`src/services/`、`src/utils/` | `08` §2.1 | `npm run dev` 可启动，访问 localhost:5173 正常 | TODO |
| TASK-S0-W0-003 | 后端依赖安装 | 创建 `requirements.txt`：Flask、pytest、black、flake8、flask-cors | `08` §1.2 | `pip install -r requirements.txt` 成功 | TODO |
| TASK-S0-W0-004 | 前端依赖安装 | 安装 axios、react-router-dom、antd（或其他 UI 库） | `08` §1.2 | `npm install` 成功，无报错 | TODO |
| TASK-S0-W0-005 | 配置质量门禁工具 | 配置 black + flake8，创建 `.flake8` 配置文件；配置 pytest | `13` §四 G-LINT | `black --check` 和 `flake8` 通过 | TODO |
| TASK-S0-W0-006 | 创建 Flask 应用入口 | 创建 `app/__init__.py`，注册蓝图，配置 CORS，设置 Base URL `/api/v1` | `09` §2.1 | Flask app 可启动在 5000 端口 | TODO |

---

## 一、S1 — 会话管理（REQ-M1QA-001 / US-001）

> **可演示目标**：会话 CRUD API + 前端会话侧栏，支持创建/删除/列表展示

### 1.1 存储层（W2）

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-S1-W2-001 | 实现 Session 数据模型 | 创建 `app/models/session.py`，定义 Session 数据结构：id、title、type、status、created_at、updated_at、message_count | `10` §2.1 | 字段与 `10` 完全一致 | TODO |
| TASK-S1-W2-002 | 实现 Session DAO — 创建 | 创建 `app/dao/session_dao.py`，实现 `create_session()`：生成 `sess_YYYYMMDD{序号}` 格式 ID，写入 `data/sessions/{id}.json`，同时创建空 `{id}_messages.json` | `10` §2.1, `06` §2.1 | JSON 文件正确写入，字段完整 | TODO |
| TASK-S1-W2-003 | 实现 Session DAO — 列表查询 | 实现 `list_sessions(limit, offset)`：读取 `data/sessions/` 目录，按 updated_at 降序排序，支持 limit（默认5，最大20）和 offset 分页 | `10` §4.2, `06` §2.2 | 排序正确，分页正确，返回 total | TODO |
| TASK-S1-W2-004 | 实现 Session DAO — 删除 | 实现 `delete_session(id)`：验证会话存在，标记 status 为 deleted，级联删除关联的 messages 和 analysis 文件 | `10` §5.1, `06` §2.3 | 状态变为 deleted，关联文件被清理 | TODO |
| TASK-S1-W2-005 | 实现 Session DAO — 详情查询 | 实现 `get_session(id)`：读取会话 JSON + 关联的 messages JSON，合并返回 | `10` §4.2, `06` §2.4 | 返回完整 Session + Messages | TODO |

### 1.2 路由层（W1）

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-S1-W1-001 | 实现通用响应格式 | 创建 `app/utils/response.py`，封装成功响应 `{code, message, data}` 和错误响应 `{code, message, error}` | `09` §2.3, RULE-001 | 响应格式与 `09` 完全一致 | TODO |
| TASK-S1-W1-002 | 实现通用错误处理 | 创建 `app/utils/errors.py`，定义错误码：400001（参数缺失）、400002（格式错误）、400003（文件不支持）、404001（会话不存在）、404002（报告不存在） | `09` §2.5 | 错误码与 `09` 完全一致 | TODO |
| TASK-S1-W1-003 | 实现 POST /sessions（API-001） | 创建 `app/api/sessions.py`，注册蓝图，实现创建会话：接收 title（可选）、type（可选），返回 201 + Session 对象 | `09` §3.1 | 请求/响应与 `09` 示例一致 | TODO |
| TASK-S1-W1-004 | 实现 GET /sessions（API-002） | 实现会话列表查询：接收 limit、offset 查询参数，返回 200 + {total, items} | `09` §3.2 | 请求/响应与 `09` 示例一致 | TODO |
| TASK-S1-W1-005 | 实现 DELETE /sessions/{id}（API-003） | 实现删除会话：路径参数 id，存在返回 200，不存在返回 404 + 错误码 404001 | `09` §3.3 | 正常/异常响应与 `09` 一致 | TODO |
| TASK-S1-W1-006 | 实现 GET /sessions/{id}（API-004） | 实现会话详情：路径参数 id，返回含 messages 列表的完整会话，不存在返回 404 | `09` §3.4 | 响应含 messages 数组 | TODO |

### 1.3 前端（W4）

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-S1-W4-001 | 实现 API 服务层 — 会话 | 创建 `src/services/sessionService.ts`，封装 createSession、listSessions、deleteSession、getSession 四个接口调用 | `09` §3 | 接口调用参数与 `09` 一致 | TODO |
| TASK-S1-W4-002 | 实现页面布局 | 创建主页面布局：左侧会话列表区 + 右侧问答区域 + 底部输入区 | `04` §6.1 | 布局与 PRD 原型一致 | TODO |
| TASK-S1-W4-003 | 实现会话列表组件 | 创建 `SessionList` 组件：展示近5个历史会话，支持点击切换、点击删除（确认弹窗）、点击新建 | `06` §6.1 | 支持创建/切换/删除操作 | TODO |
| TASK-S1-W4-004 | 实现新建会话功能 | 点击「+新建」按钮，调用 POST /sessions，创建后自动切换到新会话 | `06` §6.1 | 创建成功后列表自动刷新 | TODO |
| TASK-S1-W4-005 | 实现删除会话功能 | 点击删除按钮弹出确认弹窗，确认后调用 DELETE /sessions/{id}，删除后刷新列表 | `06` §6.1 | 删除后列表不再展示该会话 | TODO |

### 1.4 测试（W6）

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-S1-W6-001 | 创建测试 fixture | 创建 `tests/conftest.py`，配置 Flask test_client、临时数据目录、测试数据工厂 | `13` §一 | pytest 可正常运行 | TODO |
| TASK-S1-W6-002 | 存储层单元测试 | 实现 TC-M01-060～062：Session 创建/列表/删除的单元测试 | `13` §3.7 | 3 条 TC 全绿 | TODO |
| TASK-S1-W6-003 | 会话 API 集成测试 | 实现 TC-M01-020～025：创建/列表/删除/详情的集成测试，含 404 场景 | `13` §3.1 | 6 条 TC 全绿 | TODO |

---

## 二、S2 — 问答核心（REQ-M1QA-002 / US-002）

> **可演示目标**：研报上传分析 + 股票查询 + 消息发送，LLM 三级降级可用

### 2.1 存储层（W2）

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-S2-W2-001 | 实现 Message 数据模型 | 创建 `app/models/message.py`，定义 Message 结构：id、session_id、role、content、type、attachments、timestamp | `10` §2.2 | 字段与 `10` 完全一致 | TODO |
| TASK-S2-W2-002 | 实现 Message DAO | 创建消息的创建和查询方法：`create_message()`追加到 `{session_id}_messages.json`，`list_messages(session_id)` 查询消息列表 | `10` §2.2 | 消息正确追加和查询 | TODO |
| TASK-S2-W2-003 | 实现 Analysis 数据模型 | 创建 `app/models/analysis.py`，定义 Analysis 结构：id、session_id、type、status、input_data、output_data、accuracy、created_at、completed_at | `10` §2.3 | 字段与 `10` 完全一致 | TODO |
| TASK-S2-W2-004 | 实现 Analysis DAO | 创建 `app/dao/analysis_dao.py`，实现分析任务的创建、查询、更新状态 | `10` §2.3 | 分析结果正确写入 `data/analysis/` | TODO |

### 2.2 分析服务层（W3）

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-S2-W3-001 | 实现 LLM 服务 — 三级降级 | 创建 `app/services/llm_service.py`，实现 Level 0 CoPaw → Level 1 百炼 → Level 2 Demo 模式的降级链，不抛异常，响应头含 `X-LLM-Status` | `08` §4.2, `09` §7.1, REQ-M1QA-006 | 三级降级正常工作，不抛异常 | TODO |
| TASK-S2-W3-002 | 实现研报文件处理 | 创建文件处理工具：PDF/Word 文件保存到 `data/uploads/`，提取文本内容 | `06` §3.1 步骤 1-3 | 支持 PDF/DOCX 文本提取 | TODO |
| TASK-S2-W3-003 | 实现研报分析服务 | 创建 `app/services/analysis_service.py`，实现研报分析流程：验证会话 → 保存文件 → 提取文本 → 调用 LLM → 解析结果 → 多研报比对 → 保存分析 → 创建消息 | `06` §3.1, `09` §4.1 | 单/多研报分析结果正确 | TODO |
| TASK-S2-W3-004 | 实现股票查询服务 | 实现股票查询流程：验证会话 → 查询数据源 → 查询相关研报 → 调用 LLM → 计算评分 → 评估风险 → 保存结果 → 创建消息 | `06` §3.2, `09` §4.2 | 返回含 financial_indicators 的分析报告 | TODO |
| TASK-S2-W3-005 | 实现消息服务 | 创建 `app/services/session_service.py`，实现消息发送：创建用户 Message → 调用服务 → 创建助手 Message → 更新 Session | `06` §3.3 | 消息发送后返回 assistant 回复 | TODO |

### 2.3 路由层（W1）

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-S2-W1-001 | 实现 POST /analysis/report（API-005） | 创建 `app/api/analysis.py`，实现研报分析：接收 multipart/form-data（session_id、files、extract_keywords、compare_reports），返回分析结果 | `09` §4.1 | 请求/响应与 `09` 示例一致 | TODO |
| TASK-S2-W1-002 | 实现 POST /analysis/stock（API-006） | 实现股票查询：接收 JSON（session_id、stock_code、stock_name、analysis_type），返回股票分析报告 | `09` §4.2 | 请求/响应与 `09` 示例一致 | TODO |
| TASK-S2-W1-003 | 实现 POST /sessions/{id}/messages（API-007） | 实现消息发送：路径参数 id，接收 JSON（content、type），返回 assistant 回复 | `09` §4.3 | 请求/响应与 `09` 示例一致 | TODO |
| TASK-S2-W1-004 | 实现文件上传校验 | 文件类型白名单（PDF/DOC/DOCX）、大小限制（50MB）、文件名过滤特殊字符 | `11` §5.1, `09` §2.5 | 不合法文件返回 400003 | TODO |
| TASK-S2-W1-005 | 实现输入参数校验 | 会话标题长度<100、股票代码6位数字、消息内容长度<5000、分页参数边界校验 | `11` §5.2 | 参数错误返回 400001/400002 | TODO |

### 2.4 前端（W4）

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-S2-W4-001 | 实现 API 服务层 — 分析 | 创建 `src/services/analysisService.ts`，封装 analyzeReport（multipart）、analyzeStock、sendMessage 接口 | `09` §4 | 接口参数与 `09` 一致 | TODO |
| TASK-S2-W4-002 | 实现文件上传组件 | 创建 `UploadArea` 组件：拖拽/点击上传，支持多文件，文件类型校验（PDF/Word），进度条显示 | `06` §6.2, `07` §6.1 | 上传后触发分析 | TODO |
| TASK-S2-W4-003 | 实现聊天窗口组件 | 创建 `ChatWindow` 组件：消息气泡展示（user/assistant）、loading 状态、错误提示 | `06` §6.2 | 消息正确渲染 | TODO |
| TASK-S2-W4-004 | 实现底部输入区 | 文本输入框 + 发送按钮 + 上传按钮，支持 Enter 发送，空内容禁止发送 | `04` §6.1 | 输入/发送交互流畅 | TODO |
| TASK-S2-W4-005 | 实现研报分析结果展示 | 展示研报关键信息卡片：标题、摘要、核心观点、风险提示；多研报时展示比对结果（相似度、一致性） | `09` §4.1 响应 | 结果展开/收起正常 | TODO |
| TASK-S2-W4-006 | 实现股票分析结果展示 | 展示股票分析报表：财务指标、研报摘要、综合评分、风险等级 | `09` §4.2 响应 | 数据正确展示 | TODO |

### 2.5 测试（W6）

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-S2-W6-001 | Message/Analysis 单元测试 | 实现 TC-M01-063～065：消息创建/查询、分析结果创建的单元测试 | `13` §3.7 | 3 条 TC 全绿 | TODO |
| TASK-S2-W6-002 | 研报分析 API 集成测试 | 实现 TC-M01-030～034：合法 PDF、不支持格式、多研报比对、降级场景 | `13` §3.2 | 5 条 TC 全绿 | TODO |
| TASK-S2-W6-003 | 股票查询 API 集成测试 | 实现 TC-M01-035～037：合法查询、不存在代码、数据源超时 | `13` §3.3 | 3 条 TC 全绿 | TODO |
| TASK-S2-W6-004 | 消息发送 API 集成测试 | 实现 TC-M01-040～042：合法消息、空内容、不存在会话 | `13` §3.4 | 3 条 TC 全绿 | TODO |
| TASK-S2-W6-005 | LLM 降级策略测试（TDD） | 先写降级测试用例，再实现降级逻辑：CoPaw 不可用 → 百炼、全不可用 → Demo | `13` §五 | 降级链路全覆盖 | TODO |

---

## 三、S3 — 历史与追溯（REQ-M1QA-003 / US-003）

> **可演示目标**：历史会话详情 + 消息记录查看 + 上下文追溯

### 3.1 后端优化（W1 + W2）

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-S3-W2-001 | 历史查询优化 | 优化 Session 列表查询性能，确保按 updated_at 降序排序正确；确保消息列表完整返回 | `10` §4.2 | 大量数据下查询仍正常 | TODO |
| TASK-S3-W1-001 | 完善 GET /sessions/{id} 响应 | 确保返回完整 messages 列表，包含 role、content、timestamp、type、attachments | `09` §3.4 | 响应字段完整 | TODO |

### 3.2 前端（W4）

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-S3-W4-001 | 实现历史会话切换 | 点击左侧会话列表项，调用 GET /sessions/{id}，加载完整消息历史到聊天窗口 | `06` §6.1 | 切换后消息历史正确展示 | TODO |
| TASK-S3-W4-002 | 实现消息历史滚动 | 消息列表支持滚动浏览，新消息自动滚动到底部 | `06` §6.2 | 滚动交互流畅 | TODO |
| TASK-S3-W4-003 | 实现会话列表加载更多 | 列表滚动到底部时自动加载更多历史会话（offset 递增） | `06` §6.1 | 分页加载正确 | TODO |

### 3.3 测试（W6）

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-S3-W6-001 | 历史记录集成测试 | 验证 GET /sessions/{id} 返回完整消息历史，含多轮对话上下文 | `13` §3.1 TC-024~025 | TC 全绿 | TODO |

---

## 四、S4 — 报告与收口（REQ-M1QA-004 / US-004）

> **可演示目标**：报告生成下载 + 健康检查 + 全链路验收

### 4.1 报告生成（W5）

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-S4-W5-001 | 实现 Report 数据模型 | 创建 `app/models/report.py`，定义 Report 结构：id、analysis_id、session_id、title、type、content、file_path、created_at | `10` §2.4 | 字段与 `10` 完全一致 | TODO |
| TASK-S4-W5-002 | 实现报告生成服务 | 创建 `app/services/report_service.py`，实现分析结果转 PDF：读取 Analysis → 渲染 PDF → 保存到 `data/reports/` → 创建 Report 元数据 JSON | `06` §4.1 | PDF + JSON 同时生成 | TODO |
| TASK-S4-W5-003 | 实现报告 DAO | 实现报告的查询、下载逻辑：按 report_id 查询元数据、返回文件流 | `10` §2.4 | 报告可查询和下载 | TODO |

### 4.2 路由层（W1）

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-S4-W1-001 | 实现 GET /reports/{id}/download（API-008） | 创建 `app/api/reports.py`，实现报告下载：路径参数 id，查询参数 format（pdf/json），设置 Content-Type 和 Content-Disposition | `09` §5.1 | PDF 返回 application/pdf，JSON 返回 application/json | TODO |
| TASK-S4-W1-002 | 实现 GET /health（API-009） | 实现健康检查：检查数据目录可读写、LLM 服务可用性，返回 status/version/services/timestamp | `09` §5.2 | 正常→200 healthy，降级→503 degraded | TODO |

### 4.3 前端（W4）

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-S4-W4-001 | 实现 API 服务层 — 报告 | 创建 `src/services/reportService.ts`，封装 downloadReport 接口（支持 pdf/json 格式） | `09` §5.1 | 下载请求正确发送 | TODO |
| TASK-S4-W4-002 | 实现报告下载按钮 | 在分析结果展示区添加「下载 PDF」和「下载 JSON」按钮，点击触发文件下载 | `06` §4.1 | 文件自动下载到本地 | TODO |
| TASK-S4-W4-003 | 实现降级状态提示 | 当 API 返回 503 或降级响应头时，在界面展示降级提示信息 | `07` §2.3 | 降级时用户可感知 | TODO |

### 4.4 测试与验收（W6）

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-S4-W6-001 | 报告下载 API 集成测试 | 实现 TC-M01-050～052：PDF 下载、JSON 下载、报告不存在 | `13` §3.5 | 3 条 TC 全绿 | TODO |
| TASK-S4-W6-002 | 健康检查 API 集成测试 | 实现 TC-M01-055～056：正常状态、降级状态 | `13` §3.6 | 2 条 TC 全绿 | TODO |
| TASK-S4-W6-003 | 全链路验收测试 | 验证「新建会话 → 上传研报 → 查看分析 → 查看历史 → 下载报告」全流程在 3 分钟内可走通 | `04` §9, `03` §4 | 主路径 E2E 全绿 | TODO |
| TASK-S4-W6-004 | 响应体结构契约测试 | 验证所有端点的响应体结构与 `09` 一致：字段名、类型、必填项 | `13` §一 L3 Contract | RULE-001/002 验证通过 | TODO |

---

## 五、安全加固（贯穿 S1～S4）

| 任务编号 | 任务名称 | 详细内容 | 对齐 Spec | DoD（完成标准） | 状态 |
|----------|----------|----------|-----------|----------------|------|
| TASK-SEC-001 | 配置安全响应头 | 添加 X-Content-Type-Options、X-Frame-Options、X-XSS-Protection、HSTS | `11` §4.2 | 所有响应含安全头 | TODO |
| TASK-SEC-002 | 实现输出编码 | HTML 实体编码、JSON 转义、Content-Disposition 正确设置 | `11` §5.3 | XSS 测试通过 | TODO |
| TASK-SEC-003 | 实现审计日志 | 关键操作（创建/删除会话、文件上传、分析执行）记录审计日志，含时间/用户/IP/操作/结果 | `11` §6.1 | 审计日志格式与 `11` 一致 | TODO |
| TASK-SEC-004 | 实现错误信息脱敏 | 生产环境不返回堆栈信息，统一友好错误提示 | `11` §7.2 | 错误响应不含内部信息 | TODO |
| TASK-SEC-005 | 数据安全验证 | 验证研报数据本地存储、不外传；会话数据按 session_id 隔离；deleted 数据定期清理 | `11` §3, `07` §3.1 | 安全验证通过 | TODO |

---

## 六、任务统计

| 阶段 | 任务数 | 说明 |
|------|--------|------|
| S0 初始化 | 6 | 项目骨架搭建 |
| S1 会话管理 | 14 | 含存储/路由/前端/测试 |
| S2 问答核心 | 19 | 含分析服务/降级/前端 |
| S3 历史追溯 | 5 | 历史优化/前端 |
| S4 报告收口 | 11 | 报告生成/健康检查/验收 |
| 安全加固 | 5 | 贯穿全阶段 |
| **合计** | **60** | |

## 七、任务依赖关系

```
S0 初始化
  └──▶ S1 会话管理
        ├── W2 存储层 ──▶ W1 路由层 ──▶ W4 前端 ──▶ W6 测试
        └──▶ S2 问答核心
              ├── W2 存储层 ──▶ W3 分析服务 ──▶ W1 路由层 ──▶ W4 前端 ──▶ W6 测试
              └──▶ S3 历史追溯
                    └──▶ S4 报告收口
                          └── W5 报告生成 ──▶ W1 路由层 ──▶ W4 前端 ──▶ W6 验收
```

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-04-14 | 首版，基于 01～14 Spec 文档生成 60 条实施任务 |
